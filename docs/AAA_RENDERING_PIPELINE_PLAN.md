# SelfEngine AAA Rendering Pipeline Plan

## Goal

Bring SelfEngine from the current lean Forward 3D renderer toward a complete mainstream PC/console AAA rendering stack first. The current execution priority is renderer capability completion. Do not schedule new UE5 scene parsing, Unreal automation, project browser, bridge-manifest expansion, or UE viewport-parity work while the renderer gaps below remain open.

Current directive: treat SelfEngine as a standalone renderer first. The next milestones must fill missing mainstream 3A rendering capabilities, not chase `D:\UEProject`, `.uasset/.umap` parsing, UE scene reconstruction, or UE viewport matching. UE-derived assets may only be used as optional imported-model stress content if they have already been exported into ordinary formats and do not require new bridge work.

The renderer work now serves a renderer-completeness target: HDR deferred rendering with a forward residual/Forward+ path, tiled/clustered lighting, physically based materials, image-based lighting, reflection probes, production shadow atlases, screen-space AO/reflections, probe lighting, volumetrics, temporal anti-aliasing/upscaling, scalable post processing, order-independent translucency, visibility/LOD scalability, and production-grade diagnostics. Benchmark grids, imported standalone models, and hand-authored stress scenes are valid first-class validation targets while the renderer itself is being completed.

UE5 project/content preview remains a later product-integration track. Existing bridge work can stay as frozen regression coverage, but current milestones must not be blocked on parsing `.uasset/.umap`, reconstructing UE levels, exporting Unreal scenes, or matching the UE editor viewport. Once the mainstream AAA renderer is feature-complete enough, the UE project bridge can return as a validation and content-ingestion layer on top of that renderer.

This plan intentionally prioritizes the mature rendering systems used broadly across shipping high-end games before tackling extreme frontier systems. Nanite-class virtualized geometry, Lumen-class fully dynamic GI, full Virtual Shadow Maps, hardware ray tracing, and a native TSR-class upscaler are useful research targets, but they are explicitly out of scope for the current implementation track. Current SelfEngine strengths are a clean Vulkan base, PBR-ish forward shader, model import, single shadow map, culling/instancing, ImGui controls, deterministic benchmarks, and the first slices of deferred lighting/shadow/probe work. The immediate weakness is renderer feature completeness and polish: passes, attachments, descriptors, material payloads, lighting quality, temporal stability, post effects, visibility scaling, timings, debug views, and visual evidence still need to converge into one production-grade path.

Mainstream AAA anchors:

- Hybrid HDR renderer: deferred opaque lighting plus forward residual/Forward+ for translucency, particles, water, hair, special materials, and debug overlays.
- Lighting core: directional, point, spot, rect, emissive hints, tiled/clustered culling, BRDF LUT, sky irradiance, reflection probes, and local reflection fallback.
- Shadowing: cascaded directional shadows, spot/point shadow atlases, PCF/PCSS/EVSM quality tiers, contact shadows, stable filtering, and low/medium/high quality presets.
- Indirect/reflection tier: SSAO/GTAO, SSR, baked/probe GI, irradiance/light probes, reflection probes, temporal accumulation, denoisers, and clear fallback behavior.
- Atmosphere/post/transparency: volumetric fog, sky/atmosphere, bloom, tone mapping, color grading, DOF, motion blur, TAA/TAAU or FSR-style upscaling, weighted blended translucency, and eventually OIT.
- Renderer-first validation: stress the same pipeline with built-in benchmark scenes, imported standalone models, high-light-count scenes, material stress scenes, shadow/probe/SSR/AO cases, volumetric/post-process cases, and captured visual baselines.
- Production diagnostics: frame graph visibility, debug views, pass/resource timings, benchmark CSVs, RenderDoc markers, renderer reference captures, visual diffs, and measurable quality presets.
- Third-party leverage: use mature, well-maintained libraries and SDKs where they materially reduce implementation time or risk. SelfEngine should not hand-roll complex importers, compression codecs, denoisers, upscalers, shader tools, or visual-diff infrastructure when a proven library can be integrated cleanly behind an engine-owned boundary.

## Renderer Acceptance Target

The current AAA renderer milestone is considered successful when SelfEngine has the mainstream 3A rendering capabilities expected from a modern raster renderer, with measurable debug and benchmark coverage. UE5 scene parsing, project preview, Unreal export automation, and UE viewport matching are explicitly not part of the current acceptance gate.

- Main visible path: deferred opaque HDR lighting plus forward residual/Forward+ for transparent, special, particle-like, water-like, and debug materials.
- Materials: stable material ids, PBR texture payloads, normal/tangent parity, emissive, alpha mask/blend, clearcoat, transmission/volume placeholders, texture transforms, double-sided policy, and fallback diagnostics.
- Lighting and IBL: directional, point, spot, rect approximations or LTC, tiled/clustered light culling, BRDF LUT, sky irradiance, reflection probes, local probe blending, and explicit global fallback behavior.
- Shadows: CSM with stable splits/blend/fade/filtering, local point/spot shadow atlases, contact shadows, cache/invalidation policy, debug views, and quality presets.
- Indirect/reflection: SSAO moving toward GTAO, SSR with color/history/denoise tiers, probe fallback and blending, and a static probe/light-probe tier before any fully dynamic GI research.
- Atmosphere/post/temporal: volumetric fog/atmosphere, bloom, tone mapping, exposure, color grading, DOF, motion blur, TAA, TAAU/FSR-style upscaling, dynamic resolution, and sharpening.
- Visibility/scalability: classic LODs, instancing, CPU/GPU culling, Hi-Z/occlusion scaffolding, instance compaction, material bucketing, and GPU/CPU cost metrics.
- Diagnostics: every expensive feature must expose ImGui controls, render-debug views, frame-graph passes/resources, CSV metrics, quality preset state, and off/fallback behavior.
- Third-party policy: importing a capable library is allowed when it accelerates renderer completion, keeps licensing acceptable for the project, and is wrapped behind a small SelfEngine interface. Prefer established tools for formats and rendering support work, such as Assimp/tinygltf or equivalent loaders, meshoptimizer, KTX-Software/Basis Universal, DirectXTex-style texture tooling when practical, SPIRV-Reflect/SPIRV-Cross, RenderDoc markers, FSR-style upscaling SDKs, ImGuizmo/editor widgets, and image-diff libraries. Do not block progress by reimplementing these systems prematurely.
- UE bridge policy: existing UE bridge/project-preview code may remain, but it is frozen. Only keep it compiling when renderer work touches shared code. Do not add new UE scene parsing, export automation, manifest fields, project-browser behavior, or UE visual-parity gates until the renderer-first acceptance target above is substantially complete.

UE work may re-enter the plan only after this renderer gate is met:

- Deferred HDR is the default visible path for renderer-owned scenes, with forward residual/Forward+ used for transparency, special effects, water-like materials, and debug overlays.
- PBR materials, IBL, reflection probes, shadows, SSAO/GTAO, SSR, volumetrics, post, temporal AA/upscaling, LOD/occlusion, and diagnostics all have on/off controls, fallback behavior, CSV/frame-graph evidence, and smoke coverage.
- Imported generic assets (`.gltf/.glb/.fbx/.obj` or equivalent) render through the same material, lighting, shadow, post, and temporal paths as built-in benchmark scenes.
- Quality presets and reference captures make visual regressions measurable without relying on UE editor screenshots.

## Target Frame Pipeline

The final renderer should converge on this pass family:

1. Frame setup and scene update: camera history, jitter, exposure state, light lists, render settings, quality preset, and GPU budget.
2. Visibility and geometry preparation: CPU frustum culling, GPU hierarchical Z, occlusion culling, instance compaction, classic LOD selection, material bucketing, and draw preparation.
3. Depth and velocity: depth prepass where useful, motion vectors for camera/object motion, previous-frame transforms, and history-reset diagnostics.
4. Geometry scalability path: batched classic meshes, instancing, meshlet-ready metadata where cheap, CPU/GPU culling, LOD transitions, and optional visibility-buffer experiments after the deferred path is stable.
5. Shadowing: cascaded directional shadow maps, spot/point shadow atlases, PCF/PCSS/EVSM/contact-shadow quality tiers, stable filtering, cache/fallback policy.
6. GBuffer: albedo, normals, roughness/metallic/AO, emissive, velocity, material id, depth; opaque PBR defaults to deferred.
7. Lighting: tiled/clustered light culling, direct PBR lighting, image-based lighting, sky/atmosphere, shadow resolve.
8. Indirect lighting/reflections: SSAO/GTAO, SSR, reflection probes, irradiance/light probes, optional baked probe data, temporal denoise, explicit fallback tiers.
9. Forward+ path: weighted blended translucency/OIT carrier, particles, hair/fur, special materials, black-hole effect, water, debug overlays.
10. Volumetrics and atmosphere: fog, clouds, light shafts, volumetric shadows, aerial perspective.
11. Post processing: HDR scene color, auto exposure, bloom, tone mapping, color grading, DOF, motion blur, lens effects.
12. Temporal AA/upscaling: TAA first, TAAU/FSR-style upscaler second, dynamic resolution, sharpening, history rejection.
13. UI/debug/present: scene viewport, ImGui, visualizers, capture markers, screenshot/benchmark output, frame graph, and visual-diff tooling.

## Execution Roadmap

### Active Track: Complete The Mainstream AAA Renderer

Until the renderer acceptance target is met, every new implementation slice should close one of these renderer capability gaps and include visible diagnostics:

- Deferred/HDR default readiness: imported-model material parity, real velocity, robust depth handoff, exposure/tone-map consistency, and fallback-safe promotion from legacy forward.
- Lighting and IBL: BRDF LUT, sky irradiance, prefiltered cubemap path, reflection-capture resources, multi-probe blending, parallax/box projection, and real LTC rect lighting.
- Production shadows: CSM stability/filters, local point/spot atlas quality, cache invalidation, contact-shadow stability, quality presets, and visual-diff evidence.
- Screen-space/probe indirect: SSAO to GTAO, SSR history/denoise, probe fallback hierarchy, static irradiance/light probes, and unsupported-feature fallback reporting.
- Atmosphere, volumetrics, and post: height/distance fog, volumetric fog, sky/atmosphere, bloom, exposure, color grading, DOF, motion blur, lens effects, and graph-level controls.
- Temporal and upscaling: motion vectors, TAA, TAAU/FSR-style plugin boundary, dynamic resolution, sharpening, history rejection, and reset diagnostics.
- Geometry and visibility scalability: engine-native mesh cache, classic LODs, instance batching, GPU/Hi-Z occlusion, material bucketing, indirect draw preparation, and CPU/GPU cost metrics.
- Renderer tooling: benchmark scenes, RenderDoc markers, debug views, CSV/frame-graph coverage, reference captures, visual diffs, and quality-preset evidence for every expensive system.

Work can move between these bullets as dependencies require, but no new UE-specific work should be chosen as the next slice while these gaps remain.

### Frozen Track: UE Project Asset Bridge And Visual Baseline

This track is frozen until the renderer-first acceptance gate above is met. Existing bridge code and smoke tests may remain as regression coverage, but do not spend new implementation time on parsing UE5 projects, reconstructing UE levels, exporting Unreal scenes, expanding bridge manifests, or matching UE viewport screenshots while the core renderer still lacks mainstream 3A systems.

- Keep existing bridge/project-preview code compiling and avoid breaking it during renderer work.
- Do not add new `.uasset/.umap` parsing, UE automation, project browser, scene reconstruction, export-format plumbing, or UE visual parity gates in the current renderer-first track.
- Return to this track later to enumerate `D:\UEProject`, consume bridge manifests/exported scenes, preserve stable asset ids, and compare against UE viewport references after the renderer is ready enough for that comparison to be meaningful.

### Phase 0: Render-Graph Carrying Capacity

- Add an explicit frame graph/pass description layer with names, queues, resources, active/roadmap status, and debug visibility.
- Keep the existing Forward 3D output unchanged while describing current Shadow/Main/Overlay/ImGui/Present passes through the new layer.
- Extend stats and ImGui so every future feature lands as an observable pass before becoming a complex Vulkan implementation.
- Keep legacy render pass objects alive for now; introduce dynamic rendering/transient attachments only after the graph vocabulary is in place.

### Phase 1: HDR Deferred Foundation

- Introduce render targets for HDR scene color, depth, velocity, and a compact GBuffer.
- Treat imported standalone models, benchmark material grids, and renderer stress scenes as the reference workload for deferred parity while UE scene parsing is paused.
- Start with a resource-plan contract inside the frame graph: legacy physical resources and future HDR/GBuffer/history/cache resources must be visible in ImGui and benchmark CSV before Vulkan attachment migration starts.
- Allocate a real per-swapchain-image HDR scene color target first, then migrate the rendering path to write/resolve through it in a later slice.
- Add an HDR offscreen render pass and framebuffer, then record a clear-only HDR pass before scene rendering migrates, so pipeline/framebuffer lifetime can be validated separately from visual output changes.
- Allocate the first deferred target set before the GBuffer pass exists: scene depth, velocity, albedo, normal/roughness, and material buffers.
- Add a clear-only GBuffer render pass and framebuffer over those targets before migrating opaque geometry.
- Add a first GBuffer 3D geometry pipeline that writes opaque albedo, normal/roughness, material parameters, velocity placeholder, and depth while legacy forward still owns visible output.
- Add a first fullscreen deferred-lighting pass that samples the GBuffer and writes HDRSceneColor; keep swapchain presentation on legacy forward until debug/composite parity is proven.
- Add an HDR composite/debug path that samples HDRSceneColor, tone maps it, and can replace the legacy forward scene pass through a debug view or benchmark environment switch.
- Add GBuffer debug visualizers for albedo, normal, roughness, metallic, depth, velocity, and deferred shadow visibility so deferred correctness can be inspected before it becomes the default path.
- Add the first deferred shadow parity slice: reconstruct world position from depth using inverse frame matrices, sample the existing directional shadow map from deferred lighting, and keep the legacy single shadow map as the fallback tier until the production shadow-atlas work starts.
- Split opaque 3D rendering into GBuffer generation plus deferred lighting, while preserving Forward 3D as a fallback/debug path.
- Move material data from push-constant-heavy layout toward GPU material buffers with stable material ids.
- Add debug views for GBuffer channels, velocity, depth, light complexity, and overdraw.

### Phase 2: Modern Lighting Core

- Add scene light components for directional, point, spot, rect, and emissive-light hints.
- Map imported or sidecar light/sky/atmosphere parameters into scene-owned light components with explicit unsupported-field diagnostics; UE-specific light mapping is paused.
- Add the first scene-owned primary directional light to `Scene3D`, with renderer collection into `FrameLightSet` and material-push-constant fallback kept only for compatibility paths.
- Add the first scene-owned point light data model to `Scene3D`, with light revision tracking and renderer collection into `FrameLightSet`.
- Add the first scene-owned spot light data model to `Scene3D`, encoded through local-light slots with direction/type and cone-angle payloads.
- Add the first scene-owned rect light data model to `Scene3D`, encoded through local-light slots with direction/type and width/height payloads as a first-cut area-light approximation.
- Move the first directional-light data out of material push constants and into renderer-owned frame light constants so forward, deferred, shadow projection, and debug views read one authoritative light direction/intensity source.
- Introduce an explicit renderer light scaffold (`RendererLightKind`, `RendererDirectionalLight`, and `FrameLightSet`) before GPU light buffers. This gives Phase 2 a real ownership boundary and exposes total/directional/local light counts in diagnostics.
- Upload the `FrameLightSet` into a renderer-owned GPU-visible storage buffer bound from the frame descriptor set. The first buffer tier carries one directional light, ambient/specular controls, frame light counts, and fixed local-light records used by deferred lighting.
- Add a first CPU-populated tiled light assignment scaffold over the local-light record array, then replace the population step with compute culling and feed both deferred and Forward+ passes.
- Replace the single hard-coded directional light material path with renderer-owned light buffers.
- Add environment lighting: sky irradiance, reflection probes, prefiltered cubemaps, BRDF LUT, and local reflection fallback.

### Phase 3: Production Shadowing

- Upgrade the single directional shadow map into cascaded shadow maps with stable splits, per-cascade texel snapping, bias controls, and debug views.
- Use controlled sun/sky, dense local-light, and imported standalone scenes as the quality reference for shadow softness, contact grounding, cascade stability, and atlas budgets.
- Add spot and point shadow atlas support with per-light resolution budgets, cube/dual-paraboloid policy decided by quality tier, and atlas occupancy diagnostics.
- Add filtering tiers: baseline PCF, higher-quality PCSS-style soft shadows, EVSM/variance-shadow-map experiments where format support is acceptable, and contact shadows for near-field grounding.
- Preserve current single shadow map as the Low/Fallback tier until cascades and local-light shadow atlases are benchmark-proven.

### Phase 4: Screen-Space Reflections/AO + Probe Lighting

- Add SSAO first, then deepen it toward GTAO with normal/depth-aware filtering, temporal stability, and debug views.
- Add SSR for glossy reflections, with roughness-aware tracing, thickness controls, hierarchical depth traversal, temporal accumulation, and reflection-probe fallback.
- Add sky irradiance, BRDF LUT, prefiltered environment maps, local reflection probes, and probe blending volumes.
- Add generic reflection-capture import/capture, skylight cubemaps, baked probe metadata, and post-process reflection settings through engine-owned scene data; UE-specific bridge fields are deferred.
- Add a probe GI/light-probe tier for static or slowly changing indirect diffuse lighting before considering fully dynamic GI research.

### Phase 5: Mainstream Geometry/Visibility Scalability

- Convert imported meshes into an engine-native cache with stable mesh/material ids, LOD groups, bounds, material ranges, and streaming-friendly chunks.
- Preserve source asset ids when available, LOD screen-size thresholds, lightmap/UV-channel metadata, collision-independent render bounds, and instance grouping through generic import metadata.
- Add classic LOD selection, hysteresis/fading policy, instancing, GPU-driven indirect draw preparation where it reduces CPU cost, and hierarchical occlusion using depth/Hi-Z.
- Add meshlet metadata only as a compatibility-ready asset format, not as a Nanite-style virtualized renderer.
- Keep classic mesh rendering for skeletal meshes, translucent meshes, debug geometry, and unsupported materials.

### Phase 6: Temporal AA/Upscaling And Post

- Add velocity-correct TAA, then dynamic-resolution temporal upscaling.
- Add a TAAU/FSR-style plugin boundary with history, reactive masks where useful, exposure, jitter control, sharpening, and quality presets.
- Move tone mapping, exposure, bloom, color grading, motion blur, DOF, and sharpening into graph passes.
- Match captured renderer reference settings for exposure, tone mapping, color grading, fog, bloom, DOF, and motion blur closely enough that screenshot deltas are attributable to known unsupported features rather than arbitrary SelfEngine defaults.

### Phase 7: Production Renderer Tooling And Benchmarks

- Add RenderDoc-friendly debug names and capture markers for every pass/resource.
- Add a production renderer viewport/debug mode: scene list, benchmark selection, source/imported asset inspector where applicable, missing-feature report, camera bookmarks, reference screenshot selection, and SelfEngine screenshot capture.
- Add benchmark scenes: city-scale static mesh, many lights, shadow-atlas stress, SSR/AO/probe-lighting stress, volumetric/post stress, black-hole full-screen stress, heavy post/upscale, transparency/OIT stress, and imported standalone model scenes.
- Add CSV columns per pass, per resource pool, GPU memory, shadow atlas occupancy, LOD/instance counts, light count, probe count, history resets, dynamic-resolution scale, source asset count where available, missing asset count, fallback material count, unsupported feature count, and visual-diff summary metrics.
- Add quality presets: Cinematic, Epic, High, Medium, Low, and FPS-first. Every expensive system must have a measurable fallback.

### Phase 8: Renderer Feature-Complete Stabilization

- Promote deferred HDR to the default visible path only after the fallback/debug forward path, imported generic assets, material parity, lighting, shadows, probes, translucency, post, temporal stability, and diagnostics all pass smoke coverage.
- Run feature-combination benchmarks instead of single-feature demos: many lights plus CSM plus SSAO/SSR/probes, volumetrics plus transparency plus bloom, TAA/upscaling plus motion vectors, and imported-model material grids under multiple quality presets.
- Build renderer-owned reference captures and visual diffs for built-in stress scenes and generic imported scenes. These captures become the readiness proof for returning to UE integration later.
- Produce a single missing-feature report that lists unsupported renderer features, degraded fallbacks, disabled quality tiers, and known visual mismatches before any UE project-preview work resumes.

### Advanced Research Backlog

These systems are intentionally postponed until the mainstream AAA track above is usable, benchmarked, and debuggable:

- Nanite-class virtualized geometry: cluster hierarchy, cluster streaming, software/compute visibility, visibility buffers, material resolve, and automatic LOD at film-scale triangle counts.
- Lumen-class dynamic GI/reflections: surface cache/card capture, signed-distance-field or BVH tracing, software/hardware ray tracing tiers, temporal denoise, and fully dynamic multi-bounce lighting.
- Full Virtual Shadow Maps: virtual page tables, physical page atlases, page marking, invalidation/caching, directional clipmaps, local-light pages, and Nanite-scale scene integration.
- Hardware ray tracing path: BLAS/TLAS build policy, shader binding table management, ray-traced shadows/reflections/GI, denoisers, and raster fallbacks.
- Native TSR-class upscaler: deep history rejection, reactive/transparency masks, exposure-coupled resolve, async-compute scheduling, and dynamic-resolution integration beyond the first TAAU/FSR-style path.

## Implementation Rules

- Keep current demos running at every phase: `SelfEngine`, `SelfEngineForward3D`, and `SelfEngineBlackHole`.
- Keep existing UE bridge/project-preview code compiling when practical, but do not use it as a current milestone gate or primary validation scene.
- UE-specific changes are allowed only when renderer work breaks existing bridge compilation/regression coverage. Do not start new bridge functionality while mainstream renderer features are incomplete.
- Treat renderer reference scenes, imported standalone models, and stress benchmarks as the current product reference. New lighting, material, shadow, post, translucency, visibility, and temporal features must include screenshots/CSV/log diagnostics tied to renderer-controlled scenes and feature toggles.
- Do not close a phase with only a pretty screenshot. A phase closes when the feature has a visible path, an off/fallback path, debug view or ImGui control, CSV/frame-graph evidence, and at least one benchmark/smoke that exercises it.
- Do not remove the existing Forward 3D path until the deferred path can render imported textured models, shadows, debug views, and benchmark scenes.
- Use handcrafted benchmark scenes as valid current renderer proof when they are built to stress a specific mainstream 3A feature and have measurable diagnostics.
- Preserve source/import identity when available through import, renderer material records, diagnostics, screenshots, and CSV/log output so a visual mismatch can be traced back to the originating asset/material/light/scene record.
- Any unsupported material feature, light type, post-process field, texture format, geometry feature, or scene component in the active renderer/import path must produce an explicit fallback record instead of silently becoming a generic material or disappearing.
- Prefer adding thin renderer-owned concepts first: pass descriptions, render targets, material ids, light ids, history ids. Add heavyweight GPU algorithms only after the interfaces exist.
- Every phase must add a visible debug mode or benchmark metric; no invisible renderer rewrite counts as complete.
- Use Vulkan 1.3 as the baseline and gate optional mainstream paths behind feature checks: descriptor indexing, dynamic rendering, synchronization2, subgroup operations, and async compute. Mesh shaders and ray tracing remain advanced-research feature gates, not current-track requirements.
- Third-party libraries are acceptable by default when they are mature, reasonably licensed, actively maintained, and hidden behind narrow SelfEngine-owned interfaces. Add them through CMake/FetchContent or local third-party folders in a way that keeps builds reproducible, and record the reason for adoption in this plan or a small ADR when the dependency affects a major subsystem.

## Progress Snapshot

Status as of 2026-07-03: the renderer-first track is active, UE project preview remains frozen, and deferred/HDR is still an opt-in/debug-visible path rather than the default renderer.

Current completed or usable renderer carriers:

- Phase 0 diagnostics: frame-graph pass/resource visibility, stable pass/resource ids, structured resource references, access inference, pass dependency inference, resource lifetime inference, validation diagnostics, barrier/layout diagnostic planning, ImGui debug panels, benchmark CSV expansion, and feature-specific smoke evidence exist for the current major renderer slices.
- Phase 1 deferred/HDR foundation: physical HDR scene color, scene depth, compact GBuffer targets, GBuffer geometry pass, deferred lighting pass, HDR composite path, GBuffer/deferred debug views, and material-table diagnostics are in place. Legacy forward remains the default visible reference.
- Material/import parity: imported standalone model materials now cover base PBR textures, normal/tangent parity, emissive, alpha mask/blend route decisions, double-sided policy, UV transform tier, specular texture, clearcoat constants/textures/roughness texture, transmission constants/textures, and volume attenuation placeholders through the Material SSBO and benchmark diagnostics.
- Phase 2 lighting core: scene-owned directional/point/spot/rect lights, renderer-owned frame light buffer, compute-written tiled light lists, deferred local-light consumption, forward/residual light-list sharing, rect-light multi-sample approximation, and light complexity/tile occupancy debug views are implemented.
- Phase 3 shadows: CSM metadata, atlas allocation, per-cascade atlas tile rendering, atlas sampling, cascade debug view, split blend, distance fade, preset-driven PCF kernel, first PCSS-style tier, contact-shadow thickness/stability controls, and local point/spot shadow-atlas resource/tile/debug carriers exist with CSV evidence.
- Phase 4 screen-space/probe tier: first SSAO visibility term, first SSR color resolve, global reflection fallback, local reflection-probe influence blend, scene-owned reflection-probe data, renderer-owned BRDF LUT/irradiance/prefiltered IBL resource carriers, first built-in local reflection-probe cubemap sampling, and reflection/fog debug controls are implemented. These are still first production carriers, not final GTAO/SSR/probe parity.
- Forward residual/translucency: hybrid deferred opaque plus forward residual routing exists, and weighted blended translucency now has accum/revealage/write/resolve/debug/reference evidence in deferred-HDR views.
- Atmosphere/post: first analytic height/distance fog, production bloom-pyramid v1, explicit tone-map/exposure controls, GPU histogram auto exposure with eye-adaptation history, display-referred color grading with a first renderer-owned neutral LUT carrier, and first LDR sharpening tier are implemented with debug views, ImGui controls, CSV columns, frame-graph passes/resources, and on/off smoke evidence.

Known incomplete areas that still block the renderer acceptance target:

- Deferred/HDR is not default yet; real velocity, temporal stability, imported-model visual parity, feature-combination stability, and fallback safety need more proof.
- IBL/probes now have first renderer-owned BRDF LUT, irradiance cubemap, prefiltered environment cubemap carriers, scene-owned reflection-probe influence data, a fixed built-in local probe cubemap sampled by deferred, forward, and WBOIT lighting, box-projection/parallax correction, and a first top-4 multi-probe blending scaffold. Real cubemap capture/import, per-probe cubemap resources, authored sky/cubemap file sources, stronger convolution quality, and irradiance/light probes remain open.
- Post/temporal quality is still early: bloom now has a first downsample/upsample pyramid but still lacks dirt-mask/lens/anamorphic and temporal polish, auto exposure now has GPU histogram/eye-adaptation history but still lacks percentile clipping and per-volume exposure, grading has only a neutral renderer-owned LUT carrier and still lacks authored LUT import, per-volume blending, filmic look libraries, and auto-exposure coupling, sharpening is not CAS/RCAS or TAAU-coupled, and DOF/motion blur/TAA/upscaling are still missing.
- SSAO/SSR are first color/visibility tiers; GTAO filtering, hierarchical SSR traversal, temporal accumulation, denoise, history rejection, and stronger probe fallback blending remain open.
- Volumetrics are not complete; current fog is analytic height/distance fog, not volumetric fog, aerial perspective, cloud media, light shafts, or volumetric shadows.
- Visibility/scalability is still mostly classic CPU-side infrastructure; engine-native mesh cache, LOD groups, Hi-Z/occlusion, instance compaction, indirect draw preparation, and cost metrics remain open.
- Renderer tooling still needs RenderDoc markers, renderer-owned reference captures, screenshot/visual-diff automation, broader benchmark scenes, and quality-preset comparison evidence.

## Current Slice

Current execution is renderer-completion only. The FrameGraph execution-readiness diagnostic slice is now complete enough to support the next renderer features: pass/resource descriptions resolve into structured read/write refs, dependency and lifetime data are inferred internally, validation exposes missing refs/read-before-write/unused-resource/planned-resource contract issues, a diagnostic barrier/layout plan is emitted, and the first narrow execution bridge now validates the existing LightTileCull compute-to-fragment barrier against the inferred buffer transition without changing Vulkan command output. The next slices should close mainstream 3A renderer gaps in Phases 1-8: deferred/HDR completion, lighting/IBL, production shadows, SSR/AO/probes, visibility/LOD, temporal/post, volumetrics, feature-combination stabilization, and production diagnostics. The old Phase A UE project-discovery carrier exists only as frozen regression coverage; do not choose UE scene parsing, UE export, UE asset-browser behavior, or UE visual parity as upcoming work.

Active renderer backlog, in practical near-term order:

- Finish the remaining post/temporal baseline: authored/per-volume color grading on top of the neutral LUT carrier, bloom quality polish, motion blur, DOF, CAS/RCAS or TAAU-coupled sharpening, velocity correctness, TAA, history reset diagnostics, and dynamic-resolution/upscaler scaffolding.
- Deepen IBL/probes beyond the first generated carriers, scene-owned influence data, built-in cubemap sampling, box projection, and top-4 blend scaffold: authored sky/cubemap import, per-probe reflection-capture resources, dynamic/local capture policy, irradiance/light-probe data, and stronger convolution quality.
- Finish production shadow quality: CSM stability captures, local point/spot shadow atlas quality, cache invalidation, bias/filter/PCSS evidence, contact-shadow stability, and preset-level visual comparisons.
- Deepen SSAO/SSR into production tiers: GTAO-style filtering, SSR hierarchical traversal, history accumulation, denoise/rejection controls, and reflection-probe fallback blending.
- Deepen the completed first height/distance fog tier into volumetric fog, aerial perspective, fog history/stability, light scattering, volumetric shadow hooks, and stronger visual baselines.
- Build visibility/scalability: engine-native mesh cache, classic LODs, instance compaction, Hi-Z/occlusion, indirect draw preparation, material bucketing, and cost metrics.
- Complete forward residual/Forward+ feature carriers for weighted blended translucency/OIT, particles, water-like/special materials, hair-card style materials, and debug overlays.
- Expand renderer tooling: renderer-owned benchmark scenes, RenderDoc markers, screenshot/reference capture, visual-diff tooling, quality presets, and CSV coverage for every expensive system.

Next execution queue:

1. Reflection capture/probe foundation: add a real reflection-capture resource boundary, authored cubemap or captured scene source, local probe capture/blending policy, and diagnostics for capture source/fallback selection.
2. Temporal foundation: add real velocity correctness diagnostics, camera jitter/history reset state, and a first TAA resolve before attempting TAAU or dynamic resolution.
3. Visibility/scalability foundation: add mesh/instance metadata needed for classic LOD selection and Hi-Z occlusion diagnostics before moving toward GPU-driven indirect draw preparation.
4. Bloom/post polish: add dirt-mask/lens/anamorphic controls, stronger quality tiers, and captured reference evidence on top of the new bloom pyramid carrier.
5. Authored/per-volume color grading: import artist-authored LUTs, blend grading volumes, add filmic look presets, and tie grading/exposure policy into scene-owned post-process settings.

The parked bridge notes below are historical context only. They should not be used to select the next implementation slice.

- Parked UE bridge state: `src/assets/unreal_project_bridge.h/.cpp` defines the first UE project/content discovery model. It discovers project roots under `SE_UE_PROJECT_ROOT` (default `D:\UEProject`), detects `.uproject` files and `SelfEngineBridge.json` manifests, counts `.umap`, `.uasset`, exported model files, texture files, and directories, and reports explicit warnings when a bridge manifest is missing. It also consumes a bridge manifest summary: schema version, source UE project path, exported root, scene/map records, mesh/material/texture asset counts, actors, per-component mesh instances, camera records, light records, mesh export readiness/failure counts, and UE-reference capture counts. This remains useful regression coverage, but it is no longer the current execution focus.
- `nlohmann/json` is now imported through CMake `FetchContent` for manifest parsing. This is the first explicit application of the plan's third-party policy: use a maintained JSON library behind a narrow SelfEngine bridge boundary instead of hand-rolling a fragile parser.
- `scripts/Generate-SelfEngineBridgeManifest.ps1` now generates the first inventory-grade `SelfEngineBridge.json` from a UE project root. It preserves source `.uproject`, `.umap`, `.uasset`, UE virtual asset ids, planned export paths, global/reference screenshot entries, and explicit `requires_ue_editor_export` fallback status without pretending to parse raw `.uasset/.umap` internals.
- `D:\UEProject\乌云庙宇 5.8\SelfEngineBridge.json` exists as parked bridge evidence generated from the real target project. It records two map records (`/Game/001`, `/Game/NewMap`), 90 mesh-like assets, 198 material-like assets, 333 texture-like assets, 69 other assets, and one current UE screenshot reference (`Saved\AutoScreenshot.png`). UE Editor automation has filled `/Game/001` with 9 actors, 3 mesh instances, 2 lights, 1 generated reference camera, 0 post-process volumes, ready mesh exports, and reference-capture slots. This proves the bridge can carry real target-project metadata, but material graph details, true UE viewport bookmark capture, matching screenshot capture, and post-process parity are parked until after renderer completion.
- `tools/unreal/selfengine_bridge_export.py` now provides the first UE Editor Python automation entry point. When run inside Unreal Editor, it opens manifest scenes, loads maps, enumerates level actors, static/skeletal mesh components, light components, camera components, and post-process volumes, then writes actor transforms, mesh/material references, light/camera parameters, counts, and export status back into `SelfEngineBridge.json`. Exported transform records include UE-computed forward/right/up axes so SelfEngine can consume camera and light direction without guessing from Euler angles when the editor automation is available. It now also attempts to export unique static/skeletal mesh assets through UE `AssetExportTask` as `.fbx` files under `Saved\SelfEngineBridge\Exported\Meshes`, caches repeated mesh assets, and writes each component's `exportedPath`, `plannedExportedPath`, `exportFormat`, `exportStatus`, and failure message back into the manifest. If a level has no CameraComponent, it generates a stable `GeneratedReferenceCamera` from non-sky mesh bounds, records FOV/aspect/near clip/target/radius metadata, and upserts a `requires_ue_viewport_screenshot` reference-capture slot. This FBX bridge is a pragmatic first runtime geometry carrier because SelfEngine already imports through Assimp; glTF/USD should replace or supplement it once the corresponding UE exporter/plugin path is proven for better material fidelity.
- `scripts/Run-UnrealBridgeExport.ps1` wraps the UE automation with project/manifest discovery, `UE_EDITOR_CMD` or `-UnrealEditorCmd` support, and `UnrealEditor-Cmd.exe` invocation. It now also auto-discovers UE installs under `D:\Program Files\UE` and `C:\Program Files\UE`, avoiding the previous local-environment boundary for the provided UE 5.8 install at `D:\Program Files\UE\UE_5.8\Engine\Binaries\Win64\UnrealEditor-Cmd.exe`. When `-ProjectRoot` is omitted it scans `SE_UE_PROJECT_ROOT` and `D:\UEProject` for `.uproject` roots instead of relying on a Chinese path literal in the PowerShell `param` block. The script exposes `-NoMeshExport` for metadata-only runs, `-ForceMeshExport` for rebuilding exported mesh files, and `-AllowNonZeroExitWithManifestUpdate` for UE commandlet runs that write the manifest successfully but return a non-zero process code because of external environment warnings.
- `SelfEngineForward3D` supports `SE_DISCOVER_UE_PROJECTS=1` for parked bridge startup diagnostics. The real-project smoke at `out/benchmarks/aaa_real_ue_bridge_manifest_loaded_smoke.log` finds `D:\UEProject\乌云庙宇 5.8\风满楼.uproject`, loads the real `SelfEngineBridge.json`, reports `Content` with 2 `.umap` files (`001.umap`, `NewMap.umap`), 690 `.uasset` files, 192 content directories, the above asset categories, and one UE viewport/reference screenshot entry. This proves the bridge entry point exists, but renderer milestones no longer depend on this path.
- `SelfEngineForward3D` now also has a runtime bridge-scene entry: `SE_UE_BRIDGE_SCENE` selects a scene by id/name, `SE_LOAD_UE_BRIDGE_FIRST_SCENE=1` selects the first scene, and `SE_UE_BRIDGE_MANIFEST` can override the manifest path. If the selected scene has a real `exportedScenePath`, Forward3D uses `RuntimeModelLoader` to load it as the startup model. If the full-scene export is still missing, it prints the scene id/name/status and `plannedExportedScenePath` instead of silently falling back to a self-made demo. The bridge no longer depends on full-scene `.glb` as the only runtime carrier: it can reconstruct the selected scene from per-component mesh instances exported by UE.
- Runtime bridge-scene loading also understands per-component `meshInstances` with `exportedPath` and world transform data. `RuntimeModelLoader` accepts `targetMaxExtent <= 0` to preserve imported/exported scene scale instead of normalizing every asset to demo size. The parked real-project smoke at `out/benchmarks/aaa_real_ue_bridge_scene_game001_runtime_smoke.log` selects `/Game/001`, loads `3 / 3` UE-exported FBX mesh instances through Assimp, preserves their manifest transforms, and reports 3 ready / 0 missing mesh exports. Startup discovery prints top-level mesh export counts and selected-scene manifest ready/missing counts so the runtime path can tell whether the bridge has generated actual loadable files.
- Runtime bridge-scene loading now also consumes manifest `cameras` and `lights`. When a selected scene has camera records, `SelfEngineForward3D` applies the first UE camera as the initial viewport pose and FOV. When a selected scene has light records, it creates scene-owned directional, point, spot, and rect lights from the manifest instead of overwriting them every frame with a camera-derived demo key light, while SkyLight metadata routes into the environment/reflection fallback contract. The real `/Game/001` smokes apply the generated reference camera, load `3 / 3` mesh instances, apply 1 directional light, and classify the second manifest light as SkyLight rather than a point light. This path is parked as regression coverage while renderer-first work proceeds.
- `Camera3D` now has configurable near/far clip planes. Bridge scene startup can use a UE-preview-scale frustum (`near=1`, `far=500000`) so large imported scenes are not clipped by the old demo-scale `far=1000` projection. The ImGui camera panel exposes these clip planes for renderer and imported-scene inspection.
- Benchmark CSV output records parked UE bridge scene diagnostics before renderer statistics: manifest requested/loaded, scene found, full-scene export readiness, mesh instance counts, runtime mesh instance load count, mesh export readiness, camera/light counts, runtime camera/light/SkyLight application, UE reference capture count, a `ue_bridge_visual_parity_ready` legacy gate, and named blockers for missing manifests, missing scenes, no mesh instances, missing mesh exports, failed mesh loads, missing camera application, missing light application, and missing reference captures. These diagnostics remain useful for bridge regression smokes, but they are no longer current renderer acceptance gates.
- Keep the legacy swapchain/depth/shadow resources marked as physical.
- Keep HDR scene color marked as physical once its Vulkan images are allocated, even before it participates in the visible render path.
- Keep the HDR offscreen render pass visible as an active clear-only pass until scene rendering starts writing through it.
- Keep scene depth, velocity, and compact GBuffer images marked as physical once allocated.
- Keep the GBuffer pass visible as an active pass; in 3D main scenes it now writes opaque geometry into albedo, normal/roughness, material, RGB emissive, velocity, and scene depth attachments.
- Keep the deferred-lighting pass visible as an active pass; it now samples GBuffer attachments and writes HDRSceneColor through a fullscreen pipeline.
- Deferred lighting now receives inverse view/projection frame matrices, reconstructs world position from depth, samples the existing shadow map through binding 6, and applies the same first-cut PCF/ambient shadow policy used by the legacy forward shader.
- Forward, deferred, shadow projection, and shadow debug now read renderer-owned frame light constants for the first directional light. The primary source is now `Scene3D`'s scene-owned directional light; material push constants remain only as a compatibility fallback when no scene directional light exists.
- Phase 3 shadowing now has the first renderer-owned cascaded directional-shadow scaffold. `VulkanShadowSettings` carries quality-tier CSM controls (`cascadesEnabled`, stable cascade snapping, cascade count, split lambda, and max shadow distance), while `VulkanRenderer` computes per-frame camera split depths, per-cascade light view-projection matrices, and stable world-texel sizes from the current camera, primary directional light, and shadow-caster queue. The visible shadow path still renders and samples the legacy single shadow map as the Low/Fallback tier; the new cascade data is deliberately CPU/diagnostic-first so the next slice can replace the physical shadow resource with a cascade image array or atlas without reworking the renderer-facing contract.
- The first light scaffold now exists as a `FrameLightSet` with a primary directional light, a fixed-size local point-light staging array, and explicit light-kind/count fields. Diagnostics report total, directional, and local light counts as the bridge toward clustered lighting.
- `Scene3D` now owns the first real directional-light component tier: a primary `DirectionalLight3D` with direction, intensity, ambient, specular, enabled state, and light revision tracking. Forward3D updates it from the camera-derived key-light direction while continuing to mirror the same values into material properties for the legacy forward fallback.
- `Scene3D` now owns the first real local-light component tier: `PointLight3D` entries with position, radius, color, intensity, enabled state, and light revision tracking.
- `Scene3D` now also owns `SpotLight3D` entries with position, direction, radius, color, intensity, inner/outer cone angles, enabled state, and light revision tracking.
- `Scene3D` now also owns `RectLight3D` entries with position, direction, width, height, radius, color, intensity, enabled state, and light revision tracking.
- `VulkanRenderer` collects the scene-owned directional, point, spot, and rect lights into `FrameLightSet`, then uploads them into the light buffer. The debug local-light environment switch remains as an additive stress tool, not the only local-light source.
- `FrameLightSet` now uploads into a renderer-owned `VulkanLightBuffer` backed by a Vulkan storage buffer. The frame descriptor set layout binds the legacy frame UBO at binding 0 and the light storage buffer at binding 1.
- Deferred lighting reads the first directional light, ambient/specular controls, light counts, and local-light records from the GPU light storage buffer instead of relying on material push constants.
- The light storage buffer now uses a culling-friendly `GpuLocalLightRecord` / `LocalLightRecord` array instead of parallel local-light arrays. It currently reserves 64 records per frame; each record carries position/radius, color/intensity, direction/type, and packed spot/rect parameters. Forward3D's default and benchmark scenes create three scene-owned point lights, one spot light, and one rect light; `SE_DEBUG_LOCAL_LIGHTS=1` can still add three extra debug point lights for stress runs.
- The light storage buffer now carries a compute-backed tiled light-list tier: a 16px tile grid, per-tile fixed `offset/count/raw/saturated` metadata, per-tile overflow metadata, packed fixed `uvec4` local-light index groups, and a bounded scalar overflow index pool. CPU still collects scene lights, uploads the local-light records, and reports conservative capacity/fallback diagnostics, but a `LightTileCull` compute pass now writes the tile light lists before deferred lighting consumes them. The compute shader uses one invocation per tile, loops the frame-local lights, projects light bounds through the frame matrices, writes up to 16 local-light indices per tile, appends additional candidates into the bounded overflow tier, writes raw/saturation/overflow/drop metadata, and synchronizes through a compute-to-fragment barrier.
- The frame descriptor set now also carries a compact light-tile diagnostics SSBO at binding 3. The compute cull shader atomically writes saturated tile count, max raw candidates per tile, total raw tile candidates, processed tile count, overflow-backed tile count, dropped-overflow tile count, requested overflow index count, and dropped overflow index count into a 32-byte buffer. CPU readback is therefore one-frame-delayed but compact, instead of scanning the full light buffer.
- The tile-assignment tier now keeps 8192 tiles and 16 local-light references per tile as the fast fixed path, then appends candidates beyond 16 into a bounded 65536-index overflow pool. Per-tile metadata exposes fixed count, raw candidate count, overflow offset/count, and dropped overflow count. Deferred lighting consumes both fixed and overflow segments, while `Deferred Tile Occupancy` differentiates fixed-only, overflow-backed, and dropped-overflow pressure. Higher resolutions or much denser light clusters still need a larger or dynamically-resizable pool, but the current overflow smoke stores the benchmark pressure without dropping light indices.
- Deferred lighting now loops over the light buffer's local-light slots and applies the same direct PBR BRDF used by the directional light. Point lights use distance attenuation; spot lights additionally apply cone attenuation; rect lights use a first-cut finite-rectangle area approximation. These local lights are currently unshadowed, not clustered, and not consumed by the legacy forward path.
- Phase 4 now has its first screen-space ambient occlusion tier. Deferred lighting reconstructs view/world position from `SceneDepth`, reads `GBufferNormalRoughness`, evaluates a deterministic SSAO visibility term with quality-preset strength/radius/bias/sample controls, multiplies it into deferred ambient, and exposes `SE_RENDER_VIEW=ssao` / `SSAO` in ImGui. This is intentionally the first shader-integrated SSAO carrier rather than the final GTAO target: a standalone AO render target, normal/depth-aware blur, temporal accumulation, and UE post-process parity remain follow-up work.
- Runtime model import now uploads auxiliary material textures by default: metallic/roughness, normal, ambient-occlusion, and emissive maps are bound through the existing material descriptor set when present. This improves Forward and GBuffer input parity for imported PBR assets instead of silently dropping everything except the primary color texture.
- Runtime model import also carries the first imported material constant factors into the renderer-owned Material SSBO: normal-map scale, ambient-occlusion strength, and constant emissive RGB/intensity. Forward and GBuffer shaders now read those factors by frame-local material id, so imported materials without a dedicated emissive texture can still write HDR emissive into `GBufferEmissive`, and imported normal/AO textures are scaled consistently instead of being treated as hard-coded strength 1.0.
- Runtime model import now carries imported specular color/factor constants into the Material SSBO as an additional `vec4`. Forward and deferred PBR lighting use that factor to tint/scale dielectric F0 while preserving the existing metallic workflow, and `Deferred Material Table` visualizes the expanded 8-`vec4` record.
- Runtime model import now also reads glTF alpha mode/cutoff into renderer material properties. `MASK` materials stay in the deferred opaque/GBuffer route and discard pixels below the cutoff in GBuffer and forward shaders, while `BLEND` materials route into the forward residual transparency path. The Material SSBO reuses `pbrFactors.z/w` for alpha mode/cutoff, so this slice does not expand the record stride again.
- Runtime model import now carries the first primary texture UV transform into the Material SSBO: offset, scale, rotation, and enabled state. Forward and GBuffer shaders apply the transform to base color, material auxiliary, normal, occlusion, and emissive texture sampling so the forward reference and deferred material-input path stay in parity for the first transform tier. `Deferred Material Table` visualizes UV-transform materials, and benchmark CSV exposes a `frame_material_uv_transform_count` diagnostic. This is still not complete material parity: per-texture independent transforms, clearcoat coat-normal textures, independent transmission thickness maps, full refraction, and persistent material ids remain future slices.
- Runtime model import now uploads opacity textures into material descriptor binding 7 and carries them through the same texture-flag payload used by forward and GBuffer material input. Forward and GBuffer shaders multiply opacity-map red into material alpha before alpha cutoff/blend routing, while imported opacity maps without an explicit glTF alpha mode default to the transparent residual route. Benchmark CSV and ImGui expose `frame_material_opacity_texture_count`; `SE_BENCHMARK_OPACITY_TEXTURE_MATERIAL=1` and `SE_BENCHMARK_OPACITY_BLEND_TEXTURE_MATERIAL=1` exercise masked deferred and blended residual coverage without external assets.
- Runtime model import now uploads specular/specular-color textures into material descriptor binding 8 and marks them with a dedicated texture flag. Forward shading samples the specular texture RGB directly into the dielectric F0/specular-color factor path; GBuffer writes a scalar specular texture factor into the previously unused material channel, and deferred lighting consumes that factor while still reading constant specular color/factor from the Material SSBO. `Deferred Material Table` tints specular-textured materials, and benchmark CSV/ImGui expose `frame_material_specular_texture_count`; `SE_BENCHMARK_SPECULAR_TEXTURE_MATERIAL=1` exercises the path without external assets.
- Runtime model import now carries imported two-sided material policy into `MaterialProperties::doubleSided`, and the renderer records double-sided command draws through no-cull pipeline variants for the legacy forward, instanced forward, GBuffer, forward-residual, shadow, and depth-prefill paths. The Material SSBO marks double-sided records in the spare `uvControls.z` lane so `Deferred Material Table` can tint them, while benchmark CSV and ImGui expose `frame_material_double_sided_count`; `SE_BENCHMARK_DOUBLE_SIDED_MATERIAL=1` exercises this without external assets.
- Runtime model import now carries imported clearcoat factor and clearcoat roughness constants into `MaterialProperties` and the Material SSBO. The renderer packs clearcoat factor into `uvControls.w` and clearcoat roughness into `emissiveFactor.w`; forward and deferred PBR lighting add a first white clearcoat specular lobe on top of the existing direct-light BRDF, and `Deferred Material Table` tints/graphs clearcoat records. Benchmark CSV and ImGui expose `frame_material_clearcoat_count`; `SE_BENCHMARK_CLEARCOAT_MATERIAL=1` exercises the path without external assets. This is a first constant-factor tier, not full layered material support.
- Runtime model import now uploads imported clearcoat textures into material descriptor binding 9 and marks them with the clearcoat texture flag. Forward and GBuffer shaders multiply the constant clearcoat factor by the clearcoat texture red channel; GBuffer stores the resolved clearcoat factor in `GBufferEmissive.a`, and deferred lighting consumes that per-pixel value so textured clearcoat survives the deferred material-input path. `Deferred Material Table` tints clearcoat-textured records, benchmark CSV and ImGui expose `frame_material_clearcoat_texture_count`, and `SE_BENCHMARK_CLEARCOAT_TEXTURE_MATERIAL=1` exercises the path without external assets. Coat normals, layered energy compensation, and full glTF/KHR_materials_clearcoat parity remain future work.
- Runtime model import now uploads imported clearcoat roughness textures into material descriptor binding 11 and marks them with the clearcoat roughness texture flag. Forward shading multiplies the constant clearcoat roughness by the texture red channel; GBuffer writes the resolved per-pixel clearcoat roughness into the currently placeholder velocity attachment's first channel for clearcoat-roughness-textured materials, and deferred lighting consumes that value when the texture flag is present. `Deferred Material Table` tints clearcoat-roughness-textured records, benchmark CSV and ImGui expose `frame_material_clearcoat_roughness_texture_count`, and `SE_BENCHMARK_CLEARCOAT_ROUGHNESS_TEXTURE_MATERIAL=1` exercises the path without external assets. This is still a material-input parity tier, not coat normal mapping, full layered energy compensation, or complete KHR_materials_clearcoat support.
- Runtime model import now carries imported transmission factor constants into `MaterialProperties` and the Material SSBO. The renderer packs transmission factor into the spare `materialCustom.w` lane, forward and deferred PBR lighting apply a first thin-surface transmission response by blending in back-facing environment radiance while retaining the current opaque/deferred route, and `Deferred Material Table` tints/graphs transmission records. Benchmark CSV and ImGui expose `frame_material_transmission_count`; `SE_BENCHMARK_TRANSMISSION_MATERIAL=1` exercises the path without external assets. This is a first constant-factor tier, not full refraction, thickness, volume attenuation, or OIT.
- Runtime model import now uploads imported transmission textures into material descriptor binding 10 and marks them with the transmission texture flag. Forward shading multiplies the constant transmission factor by the texture red channel; GBuffer writes the resolved per-pixel transmission scalar into the currently placeholder velocity attachment's second channel for transmission-textured materials, and deferred lighting consumes that value when the texture flag is present. `Deferred Material Table` tints transmission-textured records, benchmark CSV and ImGui expose `frame_material_transmission_texture_count`, and `SE_BENCHMARK_TRANSMISSION_TEXTURE_MATERIAL=1` exercises the path without external assets. This is still a thin-surface material-input parity tier, not real refraction, thickness, volume absorption, or OIT.
- Runtime model import now carries imported volume thickness factor, attenuation distance, and attenuation color constants into `MaterialProperties` and the expanded 11-`vec4` Material SSBO. Forward and deferred lighting apply a first Beer-Lambert-style attenuation multiplier to the thin-surface transmission response, and `Deferred Material Table` can read/tint volume-capable records. Benchmark CSV and ImGui expose `frame_material_volume_count`; `SE_BENCHMARK_VOLUME_MATERIAL=1` exercises the path without external assets. This is a constant-volume material parity tier, not independent thickness textures, real refraction, volume scattering, or OIT.
- Forward and GBuffer normal-map evaluation now prefers imported vertex tangent/handedness to build the TBN basis, with the previous derivative-based tangent fallback kept only for meshes without usable tangents. This makes imported normal maps behave consistently across the forward reference path and the deferred material-input path.
- The deferred material path now has a dedicated `GBufferEmissive` RGB attachment (`R16G16B16A16_SFLOAT`). GBuffer writes imported emissive color into it, deferred lighting samples it as an HDR additive contribution, and `GBuffer Emissive` is available as a debug view through UI and `SE_RENDER_VIEW=gbuffer-emissive`.
- The GBuffer material channel now carries a frame-local material id in `GBufferMaterial.a`. `VulkanRenderer` builds one authoritative `FrameMaterialSet` for the frame, uploads it into a renderer-owned Material SSBO bound at frame descriptor binding 2, and passes the same id map into GBuffer, legacy forward, and forward-residual command recording. Deferred lighting now resolves that id into the Material SSBO and uses the record as the material-constant fallback/specular source while preserving texture-resolved GBuffer inputs. The Material SSBO now also carries the first material-classification payload: render class, packed texture flags, emissive/specular hints, alpha, and imported PBR strength factors. `MaterialProperties` has an explicit `MaterialRenderClass` (`DeferredOpaque`, `Transparent`, `ForwardSpecial`) so special and transparent materials can route without stealing unrelated shader-control floats. `GBuffer Material Id` is available through UI and `SE_RENDER_VIEW=gbuffer-material-id`, and `Deferred Material Table` / `SE_RENDER_VIEW=deferred-material-table` visualizes the Material SSBO record and class selected by each pixel. Ids are still frame-local rather than persistent asset ids.
- The first real hybrid route boundary now exists at command level: `VulkanRenderer` derives a GBuffer command list from the main 3D command queue and records only `DeferredOpaque` commands into the GBuffer pass. Transparent and forward-special commands are counted as forward residual work. Deferred HDR and deferred debug/composite views now draw those residual commands back over the fullscreen deferred result through a dedicated `ForwardResidual3D` pipeline. That pipeline uses alpha blending and disables depth writes, giving the hybrid renderer an explicit translucency/special-material carrier instead of reusing the opaque forward reference path. When residual work exists, the renderer now copies `SceneDepth` directly into the swapchain depth image and uses a depth-load main render pass so residual forward work depth-tests against deferred opaque geometry without replaying opaque geometry. The older `DepthPrefill3D` path remains as a fallback if the copy bridge cannot be used. Residual transparent commands are now sorted back-to-front within draw-order layers, while forward-special commands keep stable explicit ordering. This is still not final translucency: the residual path still needs stronger per-material depth policy, OIT or weighted blended translucency, and eventually separate special-effect pass families.
- Forward residual materials now share the same renderer-owned frame light SSBO and compute-written tiled light-list contract as deferred lighting. The forward/residual shader reads the directional light from frame constants, accumulates point/spot/rect local lights from the fixed per-tile list plus bounded overflow list, and falls back to the full local-light array if tile metadata is unavailable. `Forward Light Complexity` is available through UI and `SE_RENDER_VIEW=forward-light-complexity` for the legacy/residual forward shader path. Residual pass stats now report frame binds and shared light-list draws, giving benchmark evidence that transparent residual draws are using the same light-list carrier. This is still only a first Forward+ bridge: residual transparency still needs stronger per-material depth policy, OIT or weighted blended translucency, and separate special-effect pass families.
- Weighted blended translucency now has its first Forward+ write/resolve path: `VulkanSceneRenderTargets` allocates per-swapchain `WeightedTranslucencyAccum` (`R16G16B16A16_SFLOAT`) and `WeightedTranslucencyRevealage` (`R16_SFLOAT`) targets, `VulkanWeightedTranslucencyRenderPass`/`Framebuffer` own a two-color-attachment pass that also loads `SceneDepth` read-only, and command recording now runs `LightTileCull` before clearing accum to zero/revealage to one and drawing transparent commands into the weighted targets. The WBOIT write shader consumes the same frame light SSBO, compute-written tile lists, Material SSBO, material texture slots, CSM metadata/sampler, and local-shadow atlas metadata/sampler as the forward residual path, including normal/occlusion/specular/clearcoat/transmission/opacity inputs, directional shadows, local lights, rect-light approximation, and local-light shadow visibility. The renderer enables `independentBlend`, uses additive accum blending plus revealage multiplication, then resolves accum/revealage into `HDRSceneColor` with an alpha-blended fullscreen pass. Transparent commands are now WBOIT-only by default in deferred-HDR views; the older alpha-blended forward-residual path is kept only as an explicit visual reference when `SE_WBOIT_REFERENCE_ALPHA=1` or `SE_WEIGHTED_TRANSLUCENCY_ALPHA_REFERENCE=1` is set. `SE_RENDER_VIEW=wboit-accum`, `wboit-revealage`, and `wboit-weight` now visualize the WBOIT carrier targets through the HDR composite path, while CSV/ImGui report WBOIT debug draws and alpha-reference mismatch draw counts. The current frame graph, ImGui performance panel, and benchmark CSV expose allocation, format, extent, framebuffer count, clear, WBOIT sort, write, shared-light-list, shadow-ready, resolve, debug-view, alpha-reference, mismatch, and residual diagnostics. This is still a WBOIT quality tier rather than final translucency: real refraction/volume scattering and higher-quality per-material depth policy remain future work.
- Rect area lighting now uses a first multi-sample rectangle approximation in both deferred and forward/residual paths instead of treating rect lights as a single point with a mask. Each rect light builds tangent/bitangent axes from the scene-owned rect direction and evaluates four stratified sample points across the rectangle, feeding the same direct PBR BRDF as point/spot lights. This improves lighting spread and specular behavior while keeping the current local-light record contract. It is still not final LTC area lighting: no precomputed LTC lookup tables, horizon clipping, texture support, or shadowed rect lights exist yet.
- Add `Deferred Light Complexity`, `Deferred Tile Occupancy`, `Local Shadow Atlas`, `Local Shadow Visibility`, and `Local Shadow Face` to the render-debug view list. The first counts local lights that actually influence each pixel; the second visualizes the compute-written tile-list raw candidate count for the pixel's tile and distinguishes fixed-only, overflow-backed, and dropped-overflow tiles; the third samples the local-light shadow atlas plus tile metadata so assigned point/spot shadow tiles can be inspected; the fourth visualizes the per-pixel local-shadow visibility term used by local-light shading; the fifth colors the selected point-light cubemap face and highlights high-risk face seams. Together they expose whether tiled/clustered culling is too broad, too narrow, missing, exceeding the fixed 16-light-per-tile tier, exceeding the bounded overflow pool, failing to populate local-shadow atlas tiles, producing suspicious local-shadow visibility, or selecting unstable point-shadow faces.
- Add `Deferred HDR` to the render-debug view list. In that mode, the swapchain main pass samples HDRSceneColor and tone maps it instead of drawing the legacy forward scene.
- Add `Deferred Direct`, `Deferred Ambient`, `Deferred Specular`, `Deferred Light Complexity`, `Deferred Tile Occupancy`, `Deferred Material Table`, `Local Shadow Atlas`, `Local Shadow Visibility`, `Contact Shadow`, and `Local Shadow Face` to the render-debug view list. These views use the same deferred-lighting path as `Deferred HDR`, but isolate lighting components, local-light complexity, tile-list occupancy, per-pixel Material SSBO records, local-shadow atlas occupancy, local-shadow visibility, contact-shadow visibility, or point-shadow face/seam selection before HDR composite.
- Add `GBuffer Albedo`, `GBuffer Normal`, `GBuffer Roughness`, `GBuffer Metallic`, `GBuffer Material Id`, `GBuffer Depth`, `GBuffer Emissive`, `GBuffer Velocity`, `Deferred Shadow`, and `Shadow Cascade` to the render-debug view list. These views sample deferred attachments, reconstructed deferred shadow visibility, or CSM split/tile assignment directly into the swapchain.
- Support `SE_RENDER_VIEW=forward-light-complexity`, `deferred-hdr`, `deferred-direct`, `deferred-ambient`, `deferred-specular`, `deferred-light-complexity`, `deferred-tile-occupancy`, `deferred-material-table`, `local-shadow-atlas`, `local-shadow-visibility`, `local-shadow-face`, `point-shadow-face`, `local-shadow-seam`, `contact-shadow`, `ssao`, `ssr`, `reflection-probe`, `height-fog`, `bloom`, `tone-map`, `auto-exposure`, `color-grading`, `sharpening`, `deferred-shadow`, `shadow-cascade`, `gbuffer-albedo`, `gbuffer-normal`, `gbuffer-roughness`, `gbuffer-metallic`, `gbuffer-material-id`, `gbuffer-depth`, `gbuffer-emissive`, and `gbuffer-velocity` for benchmark/smoke runs that need to exercise visible debug paths without manual UI input.
- Keep `SelfEngine` 2D on a clear-only GBuffer target because its main pipeline is not a 3D opaque path.
- Keep `SelfEngineBlackHole` overlay geometry on the legacy forward overlay path for now; the full-screen black-hole material remains a special forward effect.
- Expose planned resources for temporal history, shadow atlases, reflection/probe data, Hi-Z/visibility buffers, and LOD/instance metadata.
- Expose the active `DirectionalCSMScaffold` pass and planned `DirectionalShadowCascades` resource in the frame graph when cascade data is generated, making the Phase 3 shadowing contract visible before the physical cascade atlas/image-array migration.
- Upload directional cascade metadata every frame through a renderer-owned `VulkanDirectionalShadowCascadeBuffer` bound at frame descriptor binding 4. The buffer carries active/configured cascade counts, split depths, stable-snapping texel sizes, per-cascade light view-projection matrices, and the current single-shadow-map fallback view-projection. Forward, deferred lighting, and GBuffer deferred-shadow debug shaders now read this buffer for split-depth cascade selection and per-cascade shadow projection.
- Allocate the first physical directional CSM atlas carrier through `VulkanDirectionalShadowCascadeAtlas`. It creates one depth atlas per swapchain image, using a 2x2 tile grid sized from the current shadow-map quality tier and capacity for the four planned cascades. The atlas is visible as a physical `DirectionalShadowCascades` frame-graph resource and has ImGui/CSV diagnostics for allocation state, tile size, atlas extent, tile grid, and cascade capacity.
- Record one shadow-depth tile pass per active directional cascade into the physical CSM atlas. `VulkanShadowFramebuffer` now supports atlas-backed depth framebuffers, `shadow_depth.vert` receives the cascade light view-projection through push constants, and command recording writes each active cascade into its 2x2 atlas tile with per-tile viewport/scissor.
- Bind the physical CSM atlas as material/GBuffer descriptor binding 6 when available, and sample it from forward, deferred lighting, and `Deferred Shadow` debug shaders. The shaders select cascades from view-space split depth, project with the selected cascade VP matrix, remap local shadow UVs into the matching 2x2 atlas tile, and clamp PCF samples to the tile boundary so neighboring cascades do not bleed into each other. The legacy single shadow map is still rendered as a transitional low/fallback resource, but the visible 3D shadow resolve now exercises the atlas path whenever active cascade data exists.
- Add a dedicated `Shadow Cascade` debug view that reuses the GBuffer debug pass to color pixels by selected directional cascade, marks pixels outside the selected cascade's shadow projection, and brightens near split/tile edges. This makes split selection, atlas coverage, and edge stability inspectable before deeper filtering work.
- Add first CSM seam softening: `VulkanShadowSettings::cascadeBlendRatio` controls a per-cascade split band, `DirectionalShadowCascadeBufferObject` uploads it through `cascadeBlendControls.x`, and forward/deferred/debug shadow resolve samples both adjacent cascade atlas tiles inside that band before blending visibility. The `Shadow Cascade` view also mixes the next cascade color inside the blend band so seam tuning is inspectable.
- Add first CSM distance fade: `VulkanShadowSettings::cascadeFadeRatio` controls the far end of the last active cascade, `DirectionalShadowCascadeBufferObject` uploads it through `cascadeBlendControls.y`, and forward/deferred/debug shadow resolve fades final-cascade visibility back to fully lit near the configured shadow distance so the shadow range does not hard-pop. The `Shadow Cascade` view fades the last cascade color toward white inside the same band.
- Add configurable PCF kernel quality for CSM resolve. `VulkanShadowSettings::pcfKernelRadius` selects 1x1, 3x3, or 5x5 sampling; quality presets keep Low at 1x1, Medium at 3x3, and High/Ultra at 5x5. The kernel radius is uploaded through `cascadeBlendControls.z`, while `pcfRadius` remains the texel spacing/radius control. Forward, deferred, and GBuffer shadow debug paths consume the same dynamic kernel.
- Add a first PCSS-style soft-shadow tier for CSM resolve. `VulkanShadowSettings::pcssStrength` is uploaded through `cascadeBlendControls.w`; Medium keeps it disabled, while High/Ultra enable a conservative blocker-search path. The shader uses a 3x3 blocker search inside the current atlas tile, estimates a penumbra from receiver/blocker depth separation, and expands the dynamic PCF radius while keeping the configured kernel size cap. This is a first soft-shadow approximation, not full production PCSS yet.
- Allocate the first physical local-light shadow atlas carrier through `VulkanLocalShadowAtlas`. It creates one sampled depth atlas per swapchain image for point/spot shadow tiles, currently using Medium 1024-pixel tiles in a 5x5 atlas grid with logical capacity for 24 local-shadow tiles. Point lights reserve six cubemap-face tiles, spot lights reserve one tile, and rect lights are excluded until a separate rect-shadow policy exists. The atlas is visible as a physical `LocalShadowAtlas` frame-graph resource, exposes an active `LocalShadowAtlasBudget` diagnostic pass, and reports allocation state, tile size, atlas extent, tile grid, point/spot counts, point face tiles, spot tiles, capacity, requested tiles, assigned tiles, and dropped tiles through ImGui and benchmark CSV.
- Record point/spot local-light shadow depth tiles into the physical local-shadow atlas. `VulkanShadowFramebuffer` now supports local-atlas-backed depth framebuffers, `PipelineSpec::ShadowDepth` uses dynamic viewport/scissor so tile writes are honored, and command recording emits one shadow-depth tile pass for each assigned point cubemap face or spot shadow tile. Point-light tiles use the six cubemap face view-projections; spot-light tiles use the scene-owned spot direction and cone angle.
- Bind the physical local-light shadow atlas and local-shadow metadata buffer to shader-visible descriptors. `VulkanLocalShadowBuffer` uploads atlas dimensions, capacity/request/drop counts, tile view-projection matrices, source local-light indices, face indices, and light kind at frame descriptor binding 5. Material/GBuffer descriptor binding 12 points at the local-shadow atlas when available. The `Local Shadow Atlas` debug view samples both the atlas texture and metadata through deferred lighting, coloring point/spot tile assignment and occupancy.
- Add the first visible point/spot local-shadow resolve. Deferred lighting and forward-residual local-light shaders now find the local-shadow tile for each point/spot local light, project the shaded point with that tile's view-projection matrix, clamp sampling inside the tile, and multiply the local light radiance by a baseline PCF visibility term. Point lights select one of the six cubemap-face tiles from the receiver direction; spot lights use their single cone tile. Rect lights remain excluded until a rect-shadow policy exists. This is a first functional resolve, not production-quality local shadowing: deeper contact shadows, stronger point-light face stability, per-light bias controls, cache policy, and renderer reference captures are still future work.
- Add independent local-shadow filtering and face-seam controls. `VulkanShadowSettings` now carries `localBiasMin`, `localBiasSlope`, `localPcfRadius`, `localPcfKernelRadius`, `localPcssStrength`, and `localFaceBlendStrength` separately from directional CSM controls. `VulkanLocalShadowBuffer` uploads bias/PCF through `filterControls`, local soft-shadow strength through `softShadowControls.x`, and point cubemap face-seam blend strength through `softShadowControls.y`. Deferred lighting and forward-residual shaders both use these controls when resolving point/spot shadow atlas visibility. Medium defaults to 3x3 PCF with `0.0009` min bias, `0.0024` slope bias, PCSS disabled, and a conservative `0.35` point-face blend; Low disables local PCF and face blend; High/Ultra use wider 5x5 local PCF, PCSS-style radius expansion, and stronger face blending. This gives local-light shadow tuning a real production control surface before cache policy and visual-diff stability captures are added.
- Add a dedicated `Local Shadow Face` debug view for point-light cubemap diagnostics. The deferred debug path picks the strongest affecting shadowed point light per pixel, colors the selected cubemap face with a stable six-color palette, and brightens pixels where the two dominant direction axes are close enough to indicate a likely face seam. It is exposed through ImGui and `SE_RENDER_VIEW=local-shadow-face` / `point-shadow-face` / `local-shadow-seam`, and benchmark CSV reports dedicated draw/frame/texture bind counters.
- Add conservative local-shadow cache optimization with partial atlas retention and scoped invalidation. `LocalShadowTile` now carries a stable cache key derived from local light identity/type/face, shadow-relevant light parameters, and a scoped caster signature. The renderer tracks tile keys per swapchain image, hashes only shadow casters that intersect each local light's influence, and further scopes point-light cubemap faces by face direction so unrelated moving casters do not invalidate every face tile. When the current swapchain image already owns a matching same-slot key for every assigned tile, command recording skips the whole local-shadow atlas clear/record pass for that image and reports skipped tiles through ImGui/CSV. When only some tiles hit, the renderer uses a depth-load shadow render pass, keeps same-slot hit tiles, clears only miss-tile rectangles, and records shadow draws only for miss tiles. If no tile hits, the renderer falls back to clearing and redrawing the full assigned atlas set. More advanced atlas defragmentation, cross-slot tile relocation, and tighter frustum/cone invalidation remain future work.
- Add deferred contact-shadow grounding and quality/stability controls. `VulkanShadowSettings` now carries `contactShadowStrength`, `contactShadowLength`, `contactShadowThickness`, `contactShadowSteps`, `contactShadowJitterStrength`, and `contactShadowEdgeFadePixels`, uploaded through `UniformBufferObject::contactShadowControls` and `contactShadowStabilityControls`. Deferred lighting ray-marches a short screen-space segment along the main light direction using the scene depth buffer, applies a tunable receiver-thickness/depth tolerance plus distance fade, uses deterministic screen-space jitter to break up fixed-step banding, fades occlusion near screen edges to avoid hard pop-in, then multiplies the result with CSM visibility before direct and ambient shadowing. The `Contact Shadow` debug view visualizes the contact-shadow visibility term directly through the deferred debug path. Medium defaults to a conservative 4-step `0.35` strength / `0.18` length / `0.08` thickness pass with `0.35` jitter and `18px` edge fade, High/Ultra increase strength, length, thickness, step count, jitter, and edge fade, and Low/Off disable the effect. This is now a stable near-field grounding tier, not a final GTAO/contact-shadow denoiser.
- Show physical/planned resource counts, GBuffer draw counts, GBuffer triangles, hybrid route draw/triangle counts, residual sort counts, GBuffer bind counts, deferred-lighting fullscreen draw/bind counts, deferred-PBR debug draw/bind counts, HDR composite draw/bind counts, GBuffer debug draw/bind counts, deferred-shadow debug draw/bind counts, shadow-cascade debug draw/bind counts, local-shadow-atlas debug draw/bind counts, local-shadow-visibility debug draw/bind counts, local-shadow-face debug draw/bind counts, contact-shadow debug draw/bind counts, shadow PCF kernel radius, shadow PCSS strength, shadow cascade blend/fade ratios, contact-shadow strength/length/thickness/step/jitter/edge-fade controls, local-shadow filter bias/PCF/PCSS/face-blend controls, local-shadow cache eligible/hit/miss/skipped counts, local-shadow buffer updates, local-shadow resolve-enabled state, local-shadow atlas allocation/tile/grid/capacity/point-count/spot-count/face-tile/occupancy/pass/draw/mesh-bind counts, light-tile compute dispatch/group counts, depth-copy operations, depth-prefill draw/mesh-bind counts, forward-residual draw/material-bind/mesh-bind counts, frame-light-constant updates, frame-light-buffer updates, frame light counts including rect-light count, frame-material-buffer updates, material counts, material capacity, material overflow counts, material class counts, material emissive/specular hint counts, material specular-texture counts, material alpha mask/blend counts, material UV-transform counts, material double-sided counts, material clearcoat counts, material clearcoat-texture counts, material clearcoat-roughness-texture counts, material transmission counts, material transmission-texture counts, material opacity-texture counts, and textured-material counts in ImGui and benchmark CSV.
- FrameGraph execution-readiness diagnostics now infer structured resource accesses, pass dependencies, resource lifetimes, validation issues, and a barrier/layout plan behind the existing small pass/resource append interface. The first execution bridge models `LightTileLists` and `LightTileDiagnostics` as physical FrameGraph buffers, has `LightTileCull` write them, has deferred/WBOIT/legacy fragment consumers read `LightTileLists`, and records the real compute-to-fragment barrier as graph-backed execution diagnostics. ImGui exposes dependency, lifetime, validation, barrier-plan, and barrier-bridge panels; benchmark CSV reports validation categories, access counts, dependency counts, lifetime counts, barrier transition/layout/queue-transfer counts, planned bridge transitions, executed graph-backed barriers, fallbacks, and mismatches. The latest bridge smoke matrix reports `framegraph_validation_issues=0`, unstructured read/write tokens `0/0`, `framegraph_barrier_buffer_transitions=3`, `framegraph_barrier_planned_bridge_transitions=3`, `framegraph_executed_barriers=1`, and `framegraph_barrier_mismatches=0` for default, deferred-HDR, SSAO, SSR, and bloom runs.
- Forward-residual diagnostics also report frame binds and shared light-list draws, so deferred composite smokes can prove transparent residual materials are executing after light-tile compute and consuming the shared light-list carrier.
- Show light tile size, tile grid dimensions, tile count, fixed assignment count/capacity, overflow assignment count/capacity, overflow tile count, dropped overflow count, CPU-side fallback/capacity-pressure count, and one-frame-delayed GPU tile diagnostics in ImGui and benchmark CSV. The GPU diagnostics read back the compact compute-written diagnostics buffer after the swapchain image's fence has completed, reporting readback validity, saturated tile count, max raw candidates per tile, total raw tile candidates, overflow-backed tile count, dropped-overflow tile count, overflow-stored indices, and dropped overflow indices. `Deferred Tile Occupancy` remains the visual per-pixel inspection path for the same raw/fixed/overflow metadata.
- Show shadow cascade configured/active counts, stable-snapping state, split lambda, max cascade distance, near/far depth range, per-cascade split depths, per-cascade world-texel sizes, physical atlas allocation/extent/tile layout/capacity, atlas tile pass/draw/mesh-bind counts, and per-frame shadow cascade buffer updates in ImGui and benchmark CSV.
- Latest lighting smoke coverage:
  - `out/benchmarks/aaa_default_scene_rect_lights_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_scene_rect_lights_smoke.csv`
  - `out/benchmarks/aaa_deferred_direct_scene_rect_lights_smoke.csv`
  - `out/benchmarks/aaa_deferred_light_complexity_smoke.csv`
  - `out/benchmarks/aaa_deferred_light_complexity_ssbo_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_light_ssbo_smoke.csv`
  - `out/benchmarks/aaa_deferred_light_complexity_records_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_light_records_smoke.csv`
  - `out/benchmarks/aaa_deferred_light_tiles_smoke.csv`
  - `out/benchmarks/aaa_deferred_light_tiles_culled_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_material_parity_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_tangent_parity_smoke.csv`
  - `out/benchmarks/aaa_gbuffer_emissive_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_rgb_emissive_smoke.csv`
  - `out/benchmarks/aaa_gbuffer_material_id_smoke.csv`
  - `out/benchmarks/aaa_deferred_shadow_view_renumber_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_material_buffer_smoke.csv`
  - `out/benchmarks/aaa_gbuffer_material_id_buffer_smoke.csv`
  - `out/benchmarks/aaa_deferred_material_classification_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_material_classification_smoke.csv`
  - `out/benchmarks/aaa_deferred_material_route_opaque_smoke.csv`
  - `out/benchmarks/aaa_deferred_material_route_transparent_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_material_route_transparent_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_forward_residual_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_forward_residual_opaque_regression.csv`
  - `out/benchmarks/aaa_deferred_material_table_forward_residual_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_forward_residual_blend_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_forward_residual_blend_opaque_regression.csv`
  - `out/benchmarks/aaa_deferred_material_table_forward_residual_blend_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_forward_residual_depth_prefill_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_depth_prefill_opaque_regression.csv`
  - `out/benchmarks/aaa_deferred_material_table_depth_prefill_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_forward_residual_depth_copy_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_depth_copy_opaque_regression.csv`
  - `out/benchmarks/aaa_deferred_material_table_depth_copy_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_forward_residual_sorted_depth_copy_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_residual_sort_opaque_regression.csv`
  - `out/benchmarks/aaa_deferred_hdr_light_tile_compute_carrier_smoke.csv` (historical compute-pipeline carrier smoke)
  - `out/benchmarks/aaa_deferred_hdr_light_tile_compute_write_smoke.csv`
  - `out/benchmarks/aaa_deferred_hdr_compute_depth_copy_residual_smoke.csv`
  - `out/benchmarks/aaa_deferred_tile_occupancy_compute_smoke.csv`
  - `out/benchmarks/aaa_deferred_material_table_tile_occupancy_renumber_smoke.csv`
  - `out/benchmarks/aaa_deferred_tile_occupancy_baseline_after_overflow_patch.csv`
  - `out/benchmarks/aaa_deferred_tile_occupancy_overflow_smoke.csv`
- `out/benchmarks/aaa_csm_scaffold_smoke.csv`
- `out/benchmarks/aaa_deferred_tile_gpu_readback_baseline_smoke.csv`
- `out/benchmarks/aaa_deferred_tile_gpu_readback_overflow_smoke.csv`
- `out/benchmarks/aaa_deferred_tile_gpu_compact_readback_baseline_smoke.csv`
- `out/benchmarks/aaa_deferred_tile_gpu_compact_readback_overflow_smoke.csv`
- `out/benchmarks/aaa_deferred_tile_overflow_tier_baseline_smoke.csv`
- `out/benchmarks/aaa_deferred_tile_overflow_tier_overflow_smoke.csv`
- `out/benchmarks/aaa_forward_residual_shared_light_list_smoke.csv`
- `out/benchmarks/aaa_forward_residual_shared_light_list_overflow_smoke.csv`
- `out/benchmarks/aaa_deferred_rect_area_quality_smoke.csv`
- `out/benchmarks/aaa_forward_residual_rect_area_quality_smoke.csv`
- `out/benchmarks/aaa_forward_constant_emissive_material_parity_smoke.csv`
- `out/benchmarks/aaa_gbuffer_constant_emissive_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_constant_emissive_parity_smoke.csv`
- `out/benchmarks/aaa_forward_specular_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_specular_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_specular_material_parity_smoke.csv`
- `out/benchmarks/aaa_forward_specular_texture_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_specular_texture_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_specular_texture_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_alpha_mask_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_hdr_alpha_blend_route_smoke.csv`
- `out/benchmarks/aaa_forward_uv_transform_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_uv_transform_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_opacity_texture_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_hdr_opacity_texture_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_hdr_opacity_texture_blend_route_smoke.csv`
- `out/benchmarks/aaa_forward_double_sided_material_policy_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_double_sided_policy_smoke.csv`
- `out/benchmarks/aaa_forward_clearcoat_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_clearcoat_material_parity_smoke.csv`
- `out/benchmarks/aaa_forward_clearcoat_texture_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_clearcoat_texture_material_parity_smoke.csv`
- `out/benchmarks/aaa_forward_clearcoat_roughness_texture_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_clearcoat_roughness_texture_material_parity_smoke.csv`
- `out/benchmarks/aaa_forward_transmission_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_transmission_material_parity_smoke.csv`
- `out/benchmarks/aaa_forward_transmission_texture_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_transmission_texture_material_parity_smoke.csv`
- `out/benchmarks/aaa_forward_volume_material_parity_smoke.csv`
- `out/benchmarks/aaa_deferred_material_table_volume_material_parity_smoke.csv`
- `out/benchmarks/aaa_gbuffer_velocity_material_payload_mask_smoke.csv`
- `out/benchmarks/aaa_csm_gpu_buffer_smoke.csv`
- `out/benchmarks/aaa_csm_atlas_resource_smoke.csv`
- `out/benchmarks/aaa_csm_atlas_tile_record_smoke.csv`
- `out/benchmarks/aaa_csm_atlas_sampling_smoke.csv`
- `out/benchmarks/aaa_csm_cascade_debug_smoke.csv`
- `out/benchmarks/aaa_csm_blend_smoke.csv`
- `out/benchmarks/aaa_csm_blend_debug_smoke.csv`
- `out/benchmarks/aaa_csm_distance_fade_smoke.csv`
- `out/benchmarks/aaa_csm_distance_fade_debug_smoke.csv`
- `out/benchmarks/aaa_csm_pcf_kernel_medium_smoke.csv`
- `out/benchmarks/aaa_csm_pcf_kernel_high_smoke.csv`
- `out/benchmarks/aaa_csm_pcss_medium_smoke.csv`
- `out/benchmarks/aaa_csm_pcss_high_smoke.csv`
- `out/benchmarks/aaa_local_shadow_atlas_resource_smoke.csv`
- `out/benchmarks/aaa_local_shadow_atlas_tile_record_smoke.csv`
- Latest CSM GPU-buffer smoke uses the default Medium shadow quality and reports 5 shadow draws, 3 configured/active directional cascades, stable snapping enabled, split lambda `0.68`, near/far cascade range `0.0100006` to `250`, split depths `26.8676 / 59.1484 / 250`, and world-texel sizes `0.031949 / 0.0703349 / 0.297281`. The same run records 8 active frame-graph passes, 10 physical resources, one deferred-lighting draw, one light-tile compute dispatch, and `shadow_cascade_buffer_updates=1` on every captured frame, proving the cascade SSBO is updated and bound while the visible renderer continues using the legacy single shadow map as the fallback tier.
- Latest CSM atlas-resource smoke keeps the same 3 active cascades and `shadow_cascade_buffer_updates=1`, while reporting 11 physical frame-graph resources and `shadow_cascade_atlas_allocated=1`. With the default Medium quality, the atlas uses `2048`-pixel tiles, a `4096x4096` 2x2 depth atlas, and capacity for 4 cascades. This proves the physical cascade atlas carrier exists and follows the shadow-quality map size, but command recording still needs to render per-cascade tiles before shaders can sample it.
- Latest CSM atlas-tile smoke keeps the same 3 active cascades, 5 legacy shadow draws, and `shadow_cascade_buffer_updates=1`, while reporting `shadow_cascade_atlas_passes=3`, `shadow_cascade_atlas_draws=15`, and `shadow_cascade_atlas_mesh_binds=9`. This proves command recording renders each active cascade into a separate atlas tile.
- Latest CSM atlas-sampling smoke (`out/benchmarks/aaa_csm_atlas_sampling_smoke.csv`) keeps the same 3 configured/active cascades, `shadow_cascade_atlas_allocated=1`, `2048`-pixel tiles, a `4096x4096` 2x2 atlas, `shadow_cascade_atlas_passes=3`, `shadow_cascade_atlas_draws=15`, `shadow_cascade_atlas_mesh_binds=9`, and `shadow_cascade_buffer_updates=1` while forward/deferred/debug descriptor binding 6 points at the physical CSM atlas. This proves the runtime path survives the switch from legacy full-map sampling to split-depth cascade selection plus atlas-tile sampling.
- Latest CSM cascade-debug smoke (`out/benchmarks/aaa_csm_cascade_debug_smoke.csv`) uses `SE_RENDER_VIEW=shadow-cascade` and reports 3 configured/active cascades, `shadow_cascade_atlas_allocated=1`, `shadow_cascade_atlas_passes=3`, `shadow_cascade_atlas_draws=15`, `shadow_cascade_atlas_mesh_binds=9`, `shadow_cascade_buffer_updates=1`, one GBuffer debug draw, and `shadow_cascade_debug_draws=1` / frame bind `1` / texture bind `1`. This proves the CSM debug view can inspect the same split-depth selection and atlas coverage path used by visible shadow resolve.
- Latest CSM blend smokes (`out/benchmarks/aaa_csm_blend_smoke.csv` and `out/benchmarks/aaa_csm_blend_debug_smoke.csv`) report default Medium `shadow_cascade_blend_ratio=0.08`, 3 active cascades, `shadow_cascade_atlas_allocated=1`, `shadow_cascade_atlas_passes=3`, `shadow_cascade_atlas_draws=15`, `shadow_cascade_buffer_updates=1`, and one deferred-lighting draw on the visible path. The debug smoke additionally records `shadow_cascade_debug_draws=1` / frame bind `1` / texture bind `1`, proving the seam-blend controls are visible in CSV and the debug view while the atlas path remains active.
- Latest CSM distance-fade smokes (`out/benchmarks/aaa_csm_distance_fade_smoke.csv` and `out/benchmarks/aaa_csm_distance_fade_debug_smoke.csv`) report default Medium `shadow_cascade_blend_ratio=0.08` and `shadow_cascade_fade_ratio=0.12`, 3 active cascades, `shadow_cascade_atlas_allocated=1`, `shadow_cascade_atlas_passes=3`, `shadow_cascade_atlas_draws=15`, `shadow_cascade_buffer_updates=1`, and one deferred-lighting draw on the visible path. The debug smoke additionally records `shadow_cascade_debug_draws=1` / frame bind `1` / texture bind `1`, proving the far-distance fade controls are visible in CSV and the `Shadow Cascade` view while the atlas path remains active.
- Latest CSM PCF-kernel smokes prove preset-driven filter quality. `out/benchmarks/aaa_csm_pcf_kernel_medium_smoke.csv` reports Medium `shadow_pcf_kernel_radius=1` (3x3), 3 active cascades, `2048`-pixel tiles, a `4096x4096` atlas, `shadow_cascade_atlas_passes=3`, and `shadow_cascade_atlas_draws=15`. `out/benchmarks/aaa_csm_pcf_kernel_high_smoke.csv` uses `SE_SHADOW_QUALITY=high` and reports `shadow_pcf_kernel_radius=2` (5x5), 4 active cascades, `4096`-pixel tiles, an `8192x8192` atlas, `shadow_cascade_atlas_passes=4`, and `shadow_cascade_atlas_draws=20`. Both retain `shadow_cascade_buffer_updates=1`, proving the dynamic PCF kernel radius is visible in CSV while the atlas path remains active.
- Latest CSM PCSS-style smokes prove the first soft-shadow tier is preset-gated. `out/benchmarks/aaa_csm_pcss_medium_smoke.csv` keeps Medium at `shadow_pcss_strength=0`, `shadow_pcf_kernel_radius=1`, 3 active cascades, and `shadow_cascade_atlas_passes=3`. `out/benchmarks/aaa_csm_pcss_high_smoke.csv` uses `SE_SHADOW_QUALITY=high` and reports `shadow_pcss_strength=0.35`, `shadow_pcf_kernel_radius=2`, 4 active cascades, `4096`-pixel tiles, an `8192x8192` atlas, `shadow_cascade_atlas_passes=4`, and `shadow_cascade_atlas_draws=20`. Both retain `shadow_cascade_buffer_updates=1`, proving the PCSS strength is visible in CSV while the atlas path remains active.
- Latest contact-shadow thickness smokes prove the near-field grounding tier has preset-controlled receiver thickness and continues to exercise the deferred debug routes. `out/benchmarks/aaa_contact_shadow_thickness_medium_smoke.csv` uses `SE_RENDER_VIEW=deferred-direct` and reports one deferred-lighting draw, Medium contact controls `shadow_contact_strength=0.35`, `shadow_contact_length=0.18`, `shadow_contact_thickness=0.08`, and `shadow_contact_steps=4`. `out/benchmarks/aaa_contact_shadow_thickness_debug_smoke.csv` uses `SE_RENDER_VIEW=contact-shadow` and reports `contact_shadow_debug_draws=1`, `contact_shadow_debug_frame_binds=1`, `contact_shadow_debug_gbuffer_binds=1`, the same Medium contact controls, and matching 232-column header/data rows. `out/benchmarks/aaa_contact_shadow_thickness_high_smoke.csv` uses `SE_SHADOW_QUALITY=high` plus `SE_RENDER_VIEW=contact-shadow`, reporting `shadow_contact_strength=0.48`, `shadow_contact_length=0.28`, `shadow_contact_thickness=0.12`, `shadow_contact_steps=6`, one deferred-lighting draw, and one contact-shadow debug draw. This proves the contact-shadow thickness lane is visible in CSV/UI, quality-preset gated, and consumed by the deferred debug path.
- Latest contact-shadow stability smokes rebuild `SelfEngineForward3D` after adding deterministic jitter and screen-edge fade controls to the deferred contact-shadow shader. `out/benchmarks/aaa_contact_shadow_stability_medium_smoke.csv` uses `SE_RENDER_VIEW=contact-shadow` and reports Medium contact controls `shadow_contact_strength=0.35`, `shadow_contact_length=0.18`, `shadow_contact_thickness=0.08`, `shadow_contact_steps=4`, `shadow_contact_jitter_strength=0.35`, `shadow_contact_edge_fade_pixels=18`, 3 active cascades, one deferred-lighting draw, and one contact-shadow debug draw/frame bind/GBuffer bind on both frames. `out/benchmarks/aaa_contact_shadow_stability_high_smoke.csv` uses `SE_SHADOW_QUALITY=high` and reports `0.48/0.28/0.12/6`, jitter `0.55`, edge fade `24`, and 4 active cascades. `out/benchmarks/aaa_contact_shadow_stability_off_smoke.csv` uses `SE_SHADOW_QUALITY=off` and reports contact strength/length/thickness/steps/jitter/edge fade all at 0 with 0 active cascades while the debug route still records one draw. All three CSV files have matching 262-column rows, and all three smoke logs are free of validation/VUID/error output.
- Latest local-shadow atlas resource smoke (`out/benchmarks/aaa_local_shadow_atlas_resource_smoke.csv`) reports `local_shadow_atlas_allocated=1`, Medium `local_shadow_atlas_tile_size=1024`, a `5120x5120` local-light depth atlas, a `5x5` tile grid with logical `local_shadow_atlas_capacity=24`, `local_shadow_shadowable_light_count=4`, `local_shadow_point_light_count=3`, `local_shadow_spot_light_count=1`, `local_shadow_point_face_tiles=18`, `local_shadow_spot_tiles=1`, `local_shadow_requested_tiles=19`, `local_shadow_assigned_tiles=19`, and `local_shadow_dropped_tiles=0` on both captured frames. The same run reports 12 physical frame-graph resources and an active `LocalShadowAtlasBudget` pass, proving the first point/spot shadow-atlas carrier and occupancy diagnostics exist before local-light shadow rendering is wired into visible lighting.
- Latest local-shadow atlas tile-record smoke (`out/benchmarks/aaa_local_shadow_atlas_tile_record_smoke.csv`) keeps Medium at `local_shadow_atlas_allocated=1`, `local_shadow_atlas_tile_size=1024`, a `5120x5120` atlas, capacity 24, 3 point lights, 1 spot light, 19 requested/assigned tiles, and 0 dropped tiles. It additionally reports `local_shadow_recorded_tile_passes=19`, `local_shadow_recorded_draws=95`, `local_shadow_recorded_mesh_binds=57`, and matching bind stats `local_shadow_atlas_passes=19`, `local_shadow_atlas_draws=95`, `local_shadow_atlas_mesh_binds=57`, proving command recording writes all assigned point/spot shadow tiles into the local atlas before visible shader sampling is enabled.
- Latest local-shadow shader-binding smoke (`out/benchmarks/aaa_local_shadow_shader_binding_smoke.csv`) uses `SE_RENDER_VIEW=local-shadow-atlas` and reports `local_shadow_atlas_allocated=1`, 19 requested/assigned local-shadow tiles, 0 dropped tiles, `local_shadow_recorded_tile_passes=19`, `local_shadow_recorded_draws=95`, `local_shadow_atlas_debug_draws=1`, `local_shadow_atlas_debug_frame_binds=1`, `local_shadow_atlas_debug_texture_binds=1`, `local_shadow_buffer_updates=1`, one deferred-PBR debug draw, and one deferred-lighting draw on both captured frames. This proves the local-shadow atlas and local-shadow tile metadata are shader-visible through the deferred debug path; full point/spot lighting shadow attenuation remains the next shadowing slice.
- Latest local-shadow deferred resolve smoke (`out/benchmarks/aaa_local_shadow_resolve_deferred_smoke.csv`) uses `SE_RENDER_VIEW=deferred-direct` and reports `local_shadow_resolve_enabled=1`, `local_shadow_atlas_allocated=1`, 19 assigned local-shadow tiles, `local_shadow_recorded_tile_passes=19`, `local_shadow_buffer_updates=1`, one deferred-PBR debug draw, and one deferred-lighting draw on both captured frames. This proves the deferred local-light shader now executes with local-shadow atlas resolve enabled.
- Latest local-shadow forward-residual resolve smoke (`out/benchmarks/aaa_local_shadow_resolve_forward_residual_smoke.csv`) uses `SE_BENCHMARK_SCENE=grid`, `SE_BENCHMARK_TRANSPARENT_MATERIAL=1`, and `SE_RENDER_VIEW=deferred-hdr`. It reports `local_shadow_resolve_enabled=1`, 19 assigned local-shadow tiles, `local_shadow_recorded_tile_passes=19`, `local_shadow_buffer_updates=1`, one deferred-lighting draw, one HDR composite draw, `forward_residual_draws=79`, `forward_residual_shared_light_list_draws=79`, and one transparent material on both captured frames. This proves the residual forward path survives the new local-shadow sampler/metadata contract and continues consuming the shared light list while local-shadow resolve is enabled.
- Latest local-shadow visibility debug smoke (`out/benchmarks/aaa_local_shadow_visibility_debug_smoke.csv`) uses `SE_RENDER_VIEW=local-shadow-visibility` and reports `local_shadow_resolve_enabled=1`, `local_shadow_visibility_debug_draws=1`, `local_shadow_visibility_debug_frame_binds=1`, `local_shadow_visibility_debug_texture_binds=1`, 19 assigned local-shadow tiles, `local_shadow_recorded_tile_passes=19`, `local_shadow_buffer_updates=1`, one deferred-PBR debug draw, and one deferred-lighting draw on both captured frames. This proves local-shadow visibility can be inspected directly before deeper bias, seam, and contact-shadow tuning.
- Latest local-shadow face debug smoke (`out/benchmarks/aaa_local_shadow_face_debug_smoke.csv`) uses `SE_RENDER_VIEW=local-shadow-face` and reports one deferred-lighting draw, one deferred-PBR debug draw, `local_shadow_face_debug_draws=1`, `local_shadow_face_debug_frame_binds=1`, `local_shadow_face_debug_texture_binds=1`, `local_shadow_resolve_enabled=1`, 19 assigned local-shadow tiles, `local_shadow_recorded_tile_passes=19`, and `local_shadow_buffer_updates=1`. The CSV has matching 226-column header/data rows. This proves point-light cubemap face selection and seam-risk visualization can be inspected and benchmarked without manual UI input.
- Latest local-shadow visibility residual smoke (`out/benchmarks/aaa_local_shadow_visibility_residual_smoke.csv`) uses `SE_BENCHMARK_SCENE=grid`, `SE_BENCHMARK_TRANSPARENT_MATERIAL=1`, and `SE_RENDER_VIEW=deferred-hdr`. It reports `local_shadow_resolve_enabled=1`, `forward_residual_draws=79`, `forward_residual_shared_light_list_draws=79`, one transparent material, 19 assigned local-shadow tiles, and `local_shadow_buffer_updates=1` on both captured frames, proving the new visibility debug instrumentation did not regress the residual forward path.
- Latest local-shadow filter-control smoke (`out/benchmarks/aaa_local_shadow_filter_controls_smoke.csv`) uses `SE_RENDER_VIEW=local-shadow-visibility` and reports `local_shadow_resolve_enabled=1`, 19 assigned local-shadow tiles, `local_shadow_recorded_tile_passes=19`, `local_shadow_visibility_debug_draws=1`, `local_shadow_buffer_updates=1`, and Medium local-shadow filter controls in CSV: `local_shadow_bias_min=0.0009`, `local_shadow_bias_slope=0.0024`, `local_shadow_pcf_radius=1`, and `local_shadow_pcf_kernel_radius=1`. This proves local-shadow bias/PCF controls are uploaded through the local-shadow metadata buffer and visible to the debug path.
- Latest local-shadow filter-control residual smoke (`out/benchmarks/aaa_local_shadow_filter_controls_residual_smoke.csv`) uses `SE_BENCHMARK_SCENE=grid`, `SE_BENCHMARK_TRANSPARENT_MATERIAL=1`, and `SE_RENDER_VIEW=deferred-hdr`. It reports `local_shadow_resolve_enabled=1`, 19 assigned local-shadow tiles, `local_shadow_recorded_tile_passes=19`, `forward_residual_draws=79`, `forward_residual_shared_light_list_draws=79`, one transparent material, and the same Medium local-shadow filter controls (`0.0009`, `0.0024`, radius `1`, kernel radius `1`). This proves the forward-residual path survives the local-shadow SSBO layout expansion and continues consuming the shared light list while local-shadow filtering controls are active.
- Latest local-shadow face-blend smokes prove the first point-light cubemap seam mitigation is preset-controlled and active on both visible paths. `out/benchmarks/aaa_local_shadow_face_blend_smoke.csv` uses `SE_RENDER_VIEW=local-shadow-visibility` and reports one deferred-PBR debug draw, `local_shadow_visibility_debug_draws=1`, `local_shadow_face_blend_strength=0.35`, 19 assigned local-shadow tiles, `local_shadow_recorded_tile_passes=19`, `local_shadow_buffer_updates=1`, and `local_shadow_resolve_enabled=1`. `out/benchmarks/aaa_local_shadow_face_blend_residual_smoke.csv` uses `SE_BENCHMARK_SCENE=grid`, `SE_BENCHMARK_TRANSPARENT_MATERIAL=1`, and `SE_RENDER_VIEW=deferred-hdr`; it reports `forward_residual_draws=79`, `forward_residual_shared_light_list_draws=79`, one transparent material, the same `local_shadow_face_blend_strength=0.35`, 19 assigned local-shadow tiles, and `local_shadow_recorded_tile_passes=19`. Both CSV files have matching 227-column header/data rows.
- Latest local-shadow cache skip smoke (`out/benchmarks/aaa_local_shadow_cache_skip_smoke.csv`) uses `SE_BENCHMARK_SCENE=grid` and `SE_RENDER_VIEW=local-shadow-visibility` across eight captured frames. Because the cache is tracked per swapchain image, frames 1-3 fill each atlas image with `local_shadow_cache_skipped_tiles=0` and `local_shadow_recorded_tile_passes=19`; once the same swapchain images recur, frames 4-8 report `local_shadow_cache_skipped_tiles=19`, `local_shadow_recorded_tile_passes=0`, and `local_shadow_recorded_draws=0`. The CSV has matching 232-column header/data rows.
- Latest local-shadow partial-retention smoke (`out/benchmarks/aaa_local_shadow_cache_partial_retention_smoke.csv`) uses `SE_BENCHMARK_SCENE=grid`, `SE_BENCHMARK_PARTIAL_LOCAL_SHADOW_CACHE=1`, and `SE_RENDER_VIEW=local-shadow-visibility` across eight captured frames. The test animates only one benchmark point light, so frames 1-3 fill each swapchain image with 19 misses and 19 recorded tile passes; frames 4-8 then report `local_shadow_cache_hit_tiles=13`, `local_shadow_cache_miss_tiles=6`, `local_shadow_cache_skipped_tiles=13`, `local_shadow_recorded_tile_passes=6`, and `local_shadow_recorded_draws=1452`. This proves same-slot cache hits are retained through the depth-load local-shadow atlas path while miss tiles are cleared and redrawn. The CSV has matching 232-column header/data rows.
- Latest local-shadow scoped dynamic smoke (`out/benchmarks/aaa_local_shadow_cache_scoped_dynamic_smoke.csv`) uses the default animated scene with `SE_RENDER_VIEW=local-shadow-visibility`. Frames 1-3 fill each swapchain image with 19 misses and 19 recorded tile passes; frames 4-8 then report `local_shadow_cache_hit_tiles=2`, `local_shadow_cache_miss_tiles=17`, `local_shadow_cache_skipped_tiles=2`, `local_shadow_recorded_tile_passes=17`, and `local_shadow_recorded_draws=85`. This proves dynamic shadow-relevant changes still invalidate affected tiles, while scoped point-face caster signatures preserve unrelated stable face tiles. The CSV has matching 232-column header/data rows.
- Latest local-shadow PCSS-style smokes prove local soft-shadow quality is preset-gated independently from CSM. `out/benchmarks/aaa_local_shadow_pcss_medium_smoke.csv` keeps Medium at `local_shadow_pcss_strength=0`, `local_shadow_pcf_kernel_radius=1`, 19 assigned local-shadow tiles, `local_shadow_recorded_tile_passes=19`, and `local_shadow_visibility_debug_draws=1`. `out/benchmarks/aaa_local_shadow_pcss_high_smoke.csv` uses `SE_SHADOW_QUALITY=high` and reports `local_shadow_pcss_strength=0.3`, `local_shadow_pcf_kernel_radius=2`, local bias `0.00075`, slope `0.002`, PCF radius `1.6`, 19 assigned local-shadow tiles, `local_shadow_recorded_tile_passes=19`, and `local_shadow_visibility_debug_draws=1`. The same high-quality run reports directional `shadow_pcss_strength=0.35` and 4 active cascades, proving local and directional PCSS controls are visible separately in CSV while the local-shadow atlas resolve path remains active.
- Latest tiled-light baseline smoke at 1280x720 reports a 16px `80x45` tile grid, 3600 tiles, 5 local lights, 9318 tile-light assignments out of 57600 fixed capacity, 0 overflow assignments, 0 dropped overflow assignments, 0 CPU-side fallback/capacity-pressure assignments, 8 active frame-graph passes including `LightTileCull`, and one compute dispatch with 10x6 workgroups before deferred lighting and HDR composite. The GPU readback path is valid, reports 0 saturated tiles, max 5 raw candidates per tile, 9318 total raw tile candidates, 0 overflow tiles, 0 overflow-stored indices, and 0 dropped overflow indices. `Deferred Tile Occupancy` records one deferred PBR debug draw while visualizing the same compute-written raw/fixed/overflow metadata.
- Latest tiled-light overflow smoke uses `SE_BENCHMARK_LIGHT_OVERFLOW=1` on the 4x4 grid scene, adding 24 large-radius benchmark point lights for 29 local lights total. It keeps the same 16px `80x45` grid and one 10x6 compute dispatch, reports 57555 fixed CPU tile-light assignments out of 57600 fixed capacity, 36003 overflow assignments across 3551 overflow-backed tiles, 0 dropped overflow assignments, and 0 CPU-side fallback/capacity-pressure events. GPU readback is valid and reports 3551 saturated tiles, 29 max raw candidates per tile, 93558 total raw tile candidates, 3551 overflow-backed tiles, 36003 overflow-stored indices, and 0 dropped overflow indices, proving the overflow tier carries this benchmark pressure instead of merely exposing fixed-list truncation.
- Latest forward-residual shared light-list smoke uses `SE_BENCHMARK_TRANSPARENT_MATERIAL=1` with `SE_RENDER_VIEW=deferred-hdr`, recording 5 hybrid forward-residual draws, 5 actual residual pass draws, 1 residual frame bind, 5 shared light-list residual draws, 5 local lights, 9318 tile-light assignments, 0 overflow assignments, a valid GPU tile readback, and one light-tile compute dispatch. With `SE_BENCHMARK_LIGHT_OVERFLOW=1`, the same transparent residual path reports 29 local lights, 57555 fixed tile assignments, 36003 overflow assignments across 3551 overflow-backed tiles, 0 dropped overflow assignments, 3551 GPU saturated/overflow-backed tiles, 36003 GPU overflow-stored indices, 0 GPU dropped overflow indices, and 5 shared light-list residual draws.
- Latest rect area quality smoke reports 5 local lights with 1 rect light, 9318 tile-light assignments, a valid GPU tile readback, one light-tile compute dispatch, and one deferred direct debug draw through `SE_RENDER_VIEW=deferred-direct`, proving the multi-sample rect path is active in the deferred shader. The forward residual rect smoke uses `SE_BENCHMARK_TRANSPARENT_MATERIAL=1` with `SE_RENDER_VIEW=deferred-hdr`, reports 5 local lights with 1 rect light, 5 hybrid/residual draws, 5 shared light-list residual draws, 9318 tile-light assignments, one compute dispatch, and a valid GPU readback, proving the same rect approximation is active in the residual forward shader path.
- Latest constant-emissive material parity smokes use `SE_BENCHMARK_CONSTANT_EMISSIVE_MATERIAL=1` on the 4x4 grid scene. The legacy forward smoke reports 17 main draws, 17 GBuffer draws, 4 frame materials, 1 emissive material hint, 1 textured material, and one light-tile compute dispatch. The `GBuffer Emissive` debug smoke reports one GBuffer debug draw, 17 GBuffer draws, 9318 tile-light assignments, 1 emissive material hint, and one compute dispatch, proving constant emissive factors reach the GBuffer path. The `Deferred Material Table` smoke reports one deferred PBR debug draw, 17 GBuffer draws, 1 emissive material hint, and one compute dispatch, proving the expanded Material SSBO record stride is readable by deferred debug views.
- Latest specular material parity smokes use `SE_BENCHMARK_SPECULAR_MATERIAL=1` on the 4x4 grid scene. The legacy forward smoke reports 17 main draws, 17 GBuffer draws, 4 frame materials, 1 specular material hint, 1 textured material, and one light-tile compute dispatch. The `Deferred Specular` smoke reports one deferred PBR debug draw, 17 GBuffer draws, 1 specular material hint, and one compute dispatch, proving the specular factor participates in deferred PBR lighting. The `Deferred Material Table` smoke reports one deferred PBR debug draw and the same 1 specular material hint, proving the expanded 8-`vec4` Material SSBO record stride is readable by deferred debug views.
- Latest specular-texture material parity smokes use `SE_BENCHMARK_SPECULAR_TEXTURE_MATERIAL=1` on the 4x4 grid scene. The legacy forward smoke reports 17 main draws, 17 GBuffer draws, 4 frame materials, 1 specular material hint, 1 specular-texture material, 2 textured materials, and one light-tile compute dispatch, proving the forward reference shader samples the specular texture slot. The `Deferred Specular` smoke reports one deferred PBR debug draw, 17 GBuffer draws, 1 specular material hint, 1 specular-texture material, and one compute dispatch, proving the GBuffer/deferred scalar specular-texture factor reaches deferred PBR. The `Deferred Material Table` smoke reports one deferred PBR debug draw with the same 1 specular-texture material and 2 textured materials, proving the expanded material descriptor contract and texture flag are visible to deferred debug views.
- Latest alpha mode/cutoff smokes use `SE_BENCHMARK_ALPHA_MASK_MATERIAL=1` and `SE_BENCHMARK_ALPHA_BLEND_MATERIAL=1` on the 4x4 grid scene. The alpha-mask material-table smoke reports 17 main draws, 17 GBuffer/deferred-opaque draws, 0 forward-residual draws, 4 opaque materials, 1 alpha-mask material, 0 alpha-blend materials, one deferred PBR debug draw, and one light-tile compute dispatch, proving masked materials remain in the deferred opaque route while carrying cutoff state. The alpha-blend deferred HDR smoke reports 12 GBuffer/deferred-opaque draws, 5 transparent/residual draws, 1 depth-copy operation, 3 opaque materials, 1 transparent material, 1 alpha-blend material, one HDR composite draw, and one light-tile compute dispatch, proving alpha blend mode uses the forward residual route.
- Latest primary UV-transform smokes use `SE_BENCHMARK_UV_TRANSFORM_MATERIAL=1` on the 4x4 grid scene. The legacy forward smoke reports 17 main draws, 17 GBuffer draws, 4 frame materials, 1 UV-transform material, 1 textured material, one deferred-lighting draw, and one light-tile compute dispatch. The `Deferred Material Table` smoke reports one deferred PBR debug draw with the same 1 UV-transform material and 1 textured material, proving the expanded 10-`vec4` Material SSBO record stride is readable by deferred debug views while forward and GBuffer texture sampling consume the same transformed UVs.
- Latest opacity-texture smokes use `SE_BENCHMARK_OPACITY_TEXTURE_MATERIAL=1` and `SE_BENCHMARK_OPACITY_BLEND_TEXTURE_MATERIAL=1` on the 4x4 grid scene. The masked material-table smoke reports 17 main draws, 17 GBuffer/deferred-opaque draws, 0 forward-residual draws, 1 alpha-mask material, 1 opacity-textured material, 2 textured materials, one deferred PBR debug draw, and one light-tile compute dispatch, proving opacity texture sampling participates in the deferred material-input path before alpha cutoff. The deferred HDR masked smoke reports one deferred-lighting draw, one HDR composite draw, 17 GBuffer draws, 1 opacity-textured material, and one light-tile compute dispatch. The opacity blend route smoke reports 11 GBuffer/deferred-opaque draws, 6 transparent/residual draws, 1 depth-copy operation, 1 alpha-blend material, 1 transparent material, 1 opacity-textured material, one HDR composite draw, and one light-tile compute dispatch, proving opacity-textured blend materials use the residual transparency path.
- Latest double-sided material policy smokes use `SE_BENCHMARK_DOUBLE_SIDED_MATERIAL=1` on the 4x4 grid scene. The legacy forward smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, 4 frame materials, 1 double-sided material, and one light-tile compute dispatch. The `Deferred Material Table` smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, one deferred PBR debug draw, 4 frame materials, 1 double-sided material, and one light-tile compute dispatch, proving the no-cull pipeline variants and Material SSBO marker are visible across the forward, instanced-forward, and deferred material-table paths.
- Latest clearcoat material parity smokes use `SE_BENCHMARK_CLEARCOAT_MATERIAL=1` on the 4x4 grid scene. The legacy forward smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, 4 frame materials, 1 clearcoat material, and one light-tile compute dispatch. The `Deferred Material Table` smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, one deferred PBR debug draw, 4 frame materials, 1 clearcoat material, and one light-tile compute dispatch, proving the constant clearcoat factor/roughness lanes are visible across the forward and deferred material-table paths.
- Latest clearcoat-texture material parity smokes use `SE_BENCHMARK_CLEARCOAT_TEXTURE_MATERIAL=1` on the 4x4 grid scene. The legacy forward smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, 4 frame materials, 1 clearcoat material, 1 clearcoat-textured material, 2 textured materials, and one light-tile compute dispatch. The `Deferred Material Table` smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, one deferred PBR debug draw, 4 frame materials, 1 clearcoat material, 1 clearcoat-textured material, 2 textured materials, and one light-tile compute dispatch, proving the clearcoat texture flag and GBuffer-resolved per-pixel clearcoat factor are visible across the forward and deferred material-table paths.
- Latest clearcoat-roughness-texture material parity smokes use `SE_BENCHMARK_CLEARCOAT_ROUGHNESS_TEXTURE_MATERIAL=1` on the 4x4 grid scene. The legacy forward smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, 4 frame materials, 1 clearcoat material, 1 clearcoat-roughness-textured material, 2 textured materials, and one light-tile compute dispatch. The `Deferred Material Table` smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, one deferred PBR debug draw, 4 frame materials, 1 clearcoat material, 1 clearcoat-roughness-textured material, 2 textured materials, and one light-tile compute dispatch, proving the clearcoat roughness texture flag and GBuffer-resolved per-pixel clearcoat roughness scalar are visible across the forward and deferred material-table paths.
- Latest transmission material parity smokes use `SE_BENCHMARK_TRANSMISSION_MATERIAL=1` on the 4x4 grid scene. The legacy forward smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, 4 frame materials, 1 transmission material, and one light-tile compute dispatch. The `Deferred Material Table` smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, one deferred PBR debug draw, 4 frame materials, 1 transmission material, and one light-tile compute dispatch, proving the constant transmission factor lane is visible across the forward and deferred material-table paths.
- Latest transmission-texture material parity smokes use `SE_BENCHMARK_TRANSMISSION_TEXTURE_MATERIAL=1` on the 4x4 grid scene. The legacy forward smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, 4 frame materials, 1 transmission material, 1 transmission-textured material, 2 textured materials, and one light-tile compute dispatch. The `Deferred Material Table` smoke reports 17 main draws, 3 instanced draws over 16 instances, 17 GBuffer draws, one deferred PBR debug draw, 4 frame materials, 1 transmission material, 1 transmission-textured material, 2 textured materials, and one light-tile compute dispatch, proving the transmission texture flag and GBuffer-resolved per-pixel transmission scalar are visible across the forward and deferred material-table paths.
- Latest volume material parity smokes use `SE_BENCHMARK_VOLUME_MATERIAL=1` on the 4x4 grid scene after rebuilding `SelfEngineForward3D`. The legacy forward smoke reports 242 main draws, 242 GBuffer draws, 4 frame materials, 1 transmission material, 1 volume material, 1 textured material, and one light-tile compute dispatch. The `Deferred Material Table` smoke reports 242 main draws, 242 GBuffer draws, one deferred PBR debug draw, one deferred-lighting draw, one HDR composite draw, 4 frame materials, 1 transmission material, 1 volume material, 1 textured material, and one light-tile compute dispatch, proving the expanded 11-`vec4` Material SSBO record and constant volume attenuation lane are visible across the forward and deferred material-table paths.
- Latest GBuffer velocity material-payload mask smoke uses clearcoat-roughness and transmission texture benchmark materials with `SE_RENDER_VIEW=gbuffer-velocity`. It reports 17 main draws, 17 GBuffer draws, one GBuffer debug draw, 1 clearcoat-roughness-textured material, 1 transmission-textured material, 3 textured materials, and one light-tile compute dispatch, proving the `GBuffer Velocity` debug view hides the temporary material payload stored in the placeholder velocity channels instead of visualizing it as real motion.
- Latest deferred HDR material-parity smoke keeps the same 16px `80x45` tile grid and 9318 assignments with 0 fallbacks, while reporting 17 GBuffer draws, one deferred-lighting draw, and one HDR composite draw.
- Latest RGB-emissive GBuffer smokes report 10 physical frame-graph resources, 17 GBuffer draws, 9318 tile-light assignments with 0 fallbacks, a working `GBuffer Emissive` debug draw, and one deferred-lighting plus one HDR composite draw on the deferred HDR path.
- Latest material-id smoke reports 10 physical frame-graph resources, 17 GBuffer draws, one GBuffer debug draw for `GBuffer Material Id`, 9318 tile-light assignments with 0 fallbacks, and the renumbered `Deferred Shadow` debug view still reports one shadow-debug draw/frame bind/texture bind.
- Latest material-classification smoke reports 4 frame materials, capacity 256, 0 overflow, 4 opaque, 0 transparent, 0 forward-special, 0 emissive hints, and 1 textured material in the 4x4 grid scene.
- Latest hybrid-route smoke keeps the default 4x4 grid at 17 main draws and 17 GBuffer/deferred-opaque draws when all materials are opaque, with 0 depth-copy operations, 0 depth-prefill draws, 0 forward-residual draws, 0 residual sort operations, and one 10x6 light-tile compute dispatch. With `SE_BENCHMARK_TRANSPARENT_MATERIAL=1`, the same scene reports 17 main draws, 12 GBuffer/deferred-opaque draws, 5 transparent forward-residual draws, 134 GBuffer triangles, 60 forward-residual triangles, 3 opaque materials, 1 transparent material, 0 overflows, one deferred-lighting draw, one HDR composite draw, one 10x6 light-tile compute dispatch, 1 direct depth-copy operation, 0 depth-prefill draws / 0 depth-prefill mesh binds, 1 residual sort operation over 5 transparent draws, and 5 draws / 1 material bind / 1 mesh bind on the dedicated alpha-blended forward-residual pass. The material-table debug path reports the same depth-copy and residual pass behavior while still recording one deferred PBR debug draw.
- Latest weighted-translucency debug/reference smokes rebuild `SelfEngineForward3D` and run transparent WBOIT carrier views plus the alpha-reference mismatch check. `out/benchmarks/aaa_wboit_accum_debug_smoke.csv`, `out/benchmarks/aaa_wboit_revealage_debug_smoke.csv`, and `out/benchmarks/aaa_wboit_weight_debug_smoke.csv` use `SE_BENCHMARK_TRANSPARENT_MATERIAL=1` with `SE_RENDER_VIEW=wboit-accum`, `wboit-revealage`, and `wboit-weight`; each reports 79 hybrid weighted-translucency draws, 79 WBOIT writes, 1 resolve, 1 WBOIT debug draw/frame bind/texture bind, 1 HDR composite draw, 1 transparent material, 0 forward-residual draws, and 0 alpha-reference mismatch draws on both captured frames. `out/benchmarks/aaa_wboit_alpha_reference_mismatch_smoke.csv` uses `SE_WBOIT_REFERENCE_ALPHA=1` with `SE_RENDER_VIEW=deferred-hdr`, keeps 79 WBOIT writes and 79 recorded forward-residual reference draws, and reports `weighted_translucency_alpha_reference_mismatch_draws=0`, proving the explicit alpha reference remains draw-count comparable to the WBOIT path. All four CSV files have matching 266-column rows, and all four smoke logs are free of validation/VUID/error output.
- Latest parked UE bridge diagnostic smoke rebuilds `SelfEngineForward3D` and runs `/Game/001` from `D:\UEProject\乌云庙宇 5.8\SelfEngineBridge.json` through `SE_LOAD_UE_BRIDGE_FIRST_SCENE=1`, `SE_RENDER_VIEW=reflection-probe`, and `out/benchmarks/aaa_ue_bridge_skylight_reflection_fallback_smoke.csv`. The smoke log applies the generated UE bridge camera, applies the UE directional light, treats the UE SkyLight as environment/reflection fallback input instead of a point light, and loads `3 / 3` mesh instances. The CSV has matching 316-column rows and records the bridge as loadable. This is useful regression evidence, but it is no longer a current renderer milestone or gate.
- Latest SSAO smokes rebuild `SelfEngineForward3D` after adding the first deferred screen-space AO tier. `out/benchmarks/aaa_ssao_debug_smoke.csv` uses `SE_BENCHMARK_SCENE=grid` with `SE_RENDER_VIEW=ssao` and reports matching 298-column rows, 12 active frame-graph passes including `SSAOIntegrated`, `ssao_enabled=1`, `ssao_strength=0.45`, `ssao_radius=1.2`, `ssao_bias=0.035`, `ssao_sample_count=8`, one deferred PBR debug draw, and `ssao_debug_draws=1`. `out/benchmarks/aaa_ue_bridge_ssao_debug_smoke.csv` runs the same view on the real `/Game/001` UE bridge scene, keeps `ue_bridge_visual_parity_ready=1`, loads `3 / 3` mesh instances, records 8 GBuffer draws, and reports the same SSAO controls plus `ssao_debug_draws=1`. `out/benchmarks/aaa_ssao_off_smoke.csv` uses `SE_SHADOW_QUALITY=off` and proves preset gating by reporting `ssao_enabled=0`, strength/radius/bias/sample count all 0, while the debug route still records one draw. All three smoke logs are free of validation/VUID/error output.
- Latest SSR smokes rebuild `SelfEngineForward3D` after promoting the first deferred screen-space reflection carrier from hit-confidence diagnostics into a conservative color contribution. Deferred lighting now traces from `SceneDepth`/`GBufferNormalRoughness`, samples the hit pixel's GBuffer albedo/normal/material/emissive payload, builds a first approximate reflected radiance, and blends it into the environment-specular term while keeping `SE_RENDER_VIEW=ssr` as the hit-confidence debug view. `out/benchmarks/aaa_ssr_color_resolve_smoke.csv` reports matching 307-column rows, 13 active frame-graph passes including `SSRIntegrated`, `ssr_enabled=1`, `ssr_color_resolve_enabled=1`, Medium controls `ssr_strength=0.55`, `ssr_ray_length=18`, `ssr_thickness=0.08`, `ssr_step_count=12`, one deferred-lighting draw, one deferred PBR debug draw, and `ssr_debug_draws=1` / frame bind `1` / GBuffer bind `1`. `out/benchmarks/aaa_ue_bridge_ssr_color_resolve_smoke.csv` runs the same view on the real `/Game/001` UE bridge scene, keeps `ue_bridge_visual_parity_ready=1`, loads `3 / 3` mesh instances, and records the same SSR color-resolve/debug evidence. `out/benchmarks/aaa_ssr_color_resolve_off_smoke.csv` uses `SE_SHADOW_QUALITY=off` and proves preset gating by reporting `ssr_enabled=0`, `ssr_color_resolve_enabled=0`, and strength/ray length/thickness/step count all 0 while the diagnostic route still records one draw. This is still the first SSR color tier, not final glossy reflection parity: hierarchical depth traversal, previous-scene-color/history sampling, temporal accumulation, denoising, and captured/multi-probe reflection fallback remain follow-up work.
- Latest reflection fallback smokes make the previous procedural sky/environment term explicit as a global reflection-probe fallback contract. The frame UBO now carries `reflectionProbeControls` for enabled state, diffuse intensity, specular intensity, and horizon blend; deferred, legacy forward, and WBOIT lighting all consume the same controls, `SE_RENDER_VIEW=reflection-probe` visualizes fallback radiance through the deferred fullscreen path, and `SE_REFLECTION_PROBE_FALLBACK=0` can disable the fallback for smoke runs. `out/benchmarks/aaa_reflection_probe_fallback_debug_smoke.csv` reports matching 314-column rows, 14 active frame-graph passes including `GlobalReflectionFallback`, `reflection_probe_fallback_enabled=1`, diffuse/specular intensity `1`, horizon blend `0.22`, and `reflection_probe_debug_draws=1` / frame bind `1` / GBuffer bind `1`. `out/benchmarks/aaa_ue_bridge_reflection_probe_fallback_debug_smoke.csv` runs the same view on the real `/Game/001` UE bridge scene, keeps `ue_bridge_visual_parity_ready=1`, loads `3 / 3` mesh instances, and records the same fallback/debug evidence. `out/benchmarks/aaa_reflection_probe_fallback_off_smoke.csv` proves the fallback is not hard-wired by reporting `reflection_probe_fallback_enabled=0` and 13 active passes while the diagnostic route still records one draw. This is not yet imported reflection captures, cubemap capture, parallax correction, or multi-probe blending; it is the measurable fallback path those features will replace.
- Latest local reflection-probe blend smokes add the first renderer-owned local probe influence volume on top of the global fallback. The frame UBO now carries local probe center/radius, enabled/intensity/blend/falloff, and color controls; deferred, legacy forward, and WBOIT environment lighting blend the procedural local probe by world-position influence, `SE_LOCAL_REFLECTION_PROBE=1` enables the path, ImGui exposes the controls, CSV reports `reflection_probe_local_*`, and the frame graph adds `LocalReflectionProbeBlend` when the local probe is applied. `out/benchmarks/aaa_local_reflection_probe_blend_smoke.csv` reports matching 321-column rows, `reflection_probe_fallback_enabled=1`, `reflection_probe_local_enabled=1`, radius `5.5`, intensity `1.25`, blend `0.65`, falloff `2`, 15 active frame-graph passes, and `reflection_probe_debug_draws=1`. `out/benchmarks/aaa_local_reflection_probe_off_smoke.csv` keeps the global fallback but reports `reflection_probe_local_enabled=0` and 14 active passes. `out/benchmarks/aaa_local_reflection_probe_fallback_off_smoke.csv` requests the local probe while setting `SE_REFLECTION_PROBE_FALLBACK=0`, reporting fallback `0`, local applied `0`, and 13 active passes, proving the local influence is measurable and gated by the reflection fallback contract. This is still a procedural local probe influence/blend tier, not real cubemap capture, parallax correction, box projection, or multi-probe blending.
- Latest IBL foundation smokes promote the earlier hidden generated IBL textures into renderer-owned, measurable resource carriers. Startup now generates a 128x128 `R16G16_SFLOAT` split-sum GGX BRDF LUT, a 32-face-size `R16G16B16A16_SFLOAT` irradiance cubemap, and a 256-face-size 5-mip `R16G16B16A16_SFLOAT` prefiltered environment cubemap, binds them at frame descriptor bindings 6-8, and samples them from deferred, legacy forward, and WBOIT environment lighting while retaining the procedural reflection fallback blend. FrameGraph exposes `BRDFLUT`, `IrradianceMap`, and `PrefilteredEnvironmentMap` as persistent physical resources, and CSV reports `ibl_*` allocation, size, format, descriptor count, and shader integration columns. `out/benchmarks/aaa_ibl_foundation_reflection_probe_smoke.csv` reports matching 439-column rows, all three IBL resources allocated, BRDF size `128`, irradiance face `32`, prefiltered face `256`, prefiltered mips `5`, 3 descriptor sets bound, shader integration `1`, one reflection-probe debug draw, one deferred-lighting draw, one HDR composite draw, 19 physical frame-graph resources, and 0 validation issues. `out/benchmarks/aaa_ibl_foundation_deferred_hdr_smoke.csv` proves the same IBL resources feed the normal deferred-HDR path with no debug draw, and `out/benchmarks/aaa_ibl_foundation_wboit_smoke.csv` keeps the same IBL evidence while recording 5 weighted-translucency draws and one WBOIT resolve. This is still a generated resource carrier and shader integration tier, not real reflection capture/import, authored sky/cubemap assets, parallax/box projection, multi-probe blending, light probes, or final convolution quality.
- Latest scene-owned reflection-probe foundation smokes move the local probe source out of renderer-only debug settings and into `Scene3D`. `ReflectionProbe3D` now carries center/radius, future box extents, color, intensity, blend strength, falloff, and enabled state; the renderer collects the first active scene probe into the frame UBO before falling back to the legacy debug probe settings, CSV reports scene probe count, active probe count, scene-owned status, radius, box extents, intensity, blend, and falloff, ImGui exposes the same live stats, and FrameGraph records `SceneReflectionProbes` plus `SceneReflectionProbeBlend` only when a scene-owned probe is consumed. `SE_SCENE_REFLECTION_PROBE=1` adds a deterministic benchmark-grid probe. `out/benchmarks/aaa_scene_reflection_probe_smoke.csv` reports matching 445-column rows, IBL resources allocated, `reflection_probe_fallback_enabled=1`, `reflection_probe_scene_probe_count=1`, `reflection_probe_active_probe_count=1`, `reflection_probe_local_scene_owned=1`, radius `6.25`, box extents `5.2 / 3.4 / 5.2`, one reflection-probe debug draw, one deferred-lighting draw, one HDR composite draw, and 0 frame-graph validation issues. `out/benchmarks/aaa_scene_reflection_probe_fallback_off_smoke.csv` keeps the scene probe present while proving the fallback gate by reporting active probe `0`, local enabled `0`, scene-owned `0`, and 0 validation issues. `out/benchmarks/aaa_scene_reflection_probe_wboit_smoke.csv` proves the same scene-owned probe path survives WBOIT with 79 weighted-translucency draws and one resolve. This is still probe influence data over the generated/global IBL path, not real cubemap capture/import, probe cubemap sampling, parallax/box projection, or multi-probe blending.
- Latest authored/procedural local reflection-probe cubemap slice adds the first actual local probe cubemap sampling path on top of the scene-owned probe data. Startup now creates a 64-face-size, 5-mip `R16G16B16A16_SFLOAT` built-in cubemap, binds it at frame descriptor binding 11 for main and overlay frame descriptor sets, marks `localReflectionProbeColor.a` when shader sampling is legal, and lets deferred, legacy forward, and WBOIT environment lighting prefer `localReflectionProbeMap` over the older tint-only local fallback. CSV reports `reflection_probe_cubemap_*`, ImGui exposes cubemap allocation/descriptors/shader state, FrameGraph exposes `SceneReflectionProbeCubemap` and records `SceneReflectionProbeCubemapSample` when a scene-owned probe consumes it, and `SE_REFLECTION_PROBE_CUBEMAP=0` can disable shader sampling while keeping the resource carrier measurable. `out/benchmarks/aaa_scene_reflection_probe_cubemap_smoke.csv` reports matching 452-column rows, `reflection_probe_cubemap_allocated=1`, face size `64`, mip count `5`, 3 cubemap descriptor sets bound, shader sampling `1`, one scene-owned active probe, one reflection-probe debug draw, and 0 frame-graph validation issues. `out/benchmarks/aaa_scene_reflection_probe_cubemap_fallback_off_smoke.csv` keeps the cubemap allocated but reports fallback `0`, active probe `0`, shader sampling `0`, and 0 validation issues. `out/benchmarks/aaa_scene_reflection_probe_cubemap_wboit_smoke.csv` proves the same scene-owned cubemap path survives deferred-HDR WBOIT with shader sampling `1`, 5 weighted-translucency draws, one resolve, and 0 validation issues. This is still a fixed built-in/procedural cubemap carrier, not HDRI import, UE reflection-capture import, dynamic scene capture, parallax/box projection, or multi-probe blending.
- Latest reflection-capture resource-boundary smokes put an explicit capture-source model around the local probe cubemap carrier. `ReflectionProbe3D` now carries `None`, `BuiltInProcedural`, `AuthoredCubemap`, or `CapturedScene` source intent, `VulkanReflectionProbeResources` owns the renderer built-in cubemap/view/descriptor-boundary state, `SE_REFLECTION_PROBE_CAPTURE_SOURCE` can force source selection for diagnostics, CSV reports `reflection_probe_capture_source_type`, `reflection_probe_capture_resource_ready`, `reflection_probe_capture_fallback_reason`, and `reflection_probe_capture_descriptor_bound`, ImGui shows source/readiness/fallback, and FrameGraph exposes `ReflectionCaptureSource` separately from `SceneReflectionProbeCubemap`. `out/benchmarks/aaa_reflection_capture_boundary_builtin_smoke.csv` reports 456-column rows, 0 validation issues, source `1`, resource ready `1`, fallback reason `0`, descriptor bound `1`, cubemap allocated `1`, 3 cubemap descriptors, and shader sampling `1`. `out/benchmarks/aaa_reflection_capture_boundary_authored_fallback_smoke.csv` forces authored cubemap source and reports source `2`, resource ready `0`, fallback reason `2`, descriptor bound `0`, cubemap carrier allocated `1`, shader sampling `0`, and 0 validation issues. `out/benchmarks/aaa_reflection_capture_boundary_wboit_smoke.csv` proves the built-in capture boundary survives deferred-HDR WBOIT with source ready `1`, shader sampling `1`, 5 weighted-translucency draws, one resolve, one deferred-lighting draw, one HDR composite draw, and 0 validation issues. This is the source/resource/fallback boundary for future imported HDRI/UE capture/dynamic scene capture work; authored cubemap loading, real scene capture, parallax/box projection, multi-probe blending, and capture refresh policy remain future slices.
- Latest reflection-probe spatial-policy smokes add the first box-projection/parallax correction and single-probe selection policy on top of the capture boundary. The frame UBO now carries local probe box extents plus a projection enable flag, deferred/legacy-forward/WBOIT shaders fade local influence by both sphere radius and box extent and box-project the local cubemap lookup direction before sampling, the renderer scores scene probes against the current camera and selects one eligible probe while reporting dropped candidates, and FrameGraph records `SceneReflectionProbeSelection` before `ReflectionCaptureSourceResolve`. CSV reports `reflection_probe_scene_eligible_probe_count`, `reflection_probe_selected_probe_index`, `reflection_probe_dropped_probe_count`, `reflection_probe_box_projection_enabled`, `reflection_probe_influence_mode`, and `reflection_probe_parallax_correction_enabled`; `SE_SCENE_REFLECTION_PROBE_MULTI=1` creates a deterministic three-probe benchmark set for selection smokes. `out/benchmarks/aaa_reflection_probe_spatial_policy_box_smoke.csv` reports 462-column rows, one probe, one eligible probe, selected index `0`, dropped `0`, box projection `1`, parallax correction `1`, shader sampling `1`, and 0 validation issues. `out/benchmarks/aaa_reflection_probe_spatial_policy_multi_smoke.csv` reports three scene probes, three eligible probes, selected index `0`, dropped `2`, shader sampling `1`, and 0 validation issues. `out/benchmarks/aaa_reflection_probe_spatial_policy_wboit_smoke.csv` proves the spatial policy survives deferred-HDR WBOIT with three probes, dropped `2`, box/parallax enabled, shader sampling `1`, 5 weighted-translucency draws, one resolve, and 0 validation issues. `out/benchmarks/aaa_reflection_probe_spatial_policy_authored_fallback_smoke.csv` forces authored source and reports source `2`, ready `0`, fallback reason `2`, descriptor bound `0`, box/parallax diagnostics still present, shader sampling `0`, and 0 validation issues. This is still single selected-probe sampling, not blended multi-probe cubemaps, authored cubemap import, dynamic capture refresh, or final capture priority volumes.
- Latest reflection-probe multi-blend scaffold smokes upgrade scene-owned probe selection from one best probe to a top-4 selected set. `FrameReflectionProbeSet` now carries selected probe arrays and blend diagnostics, the frame UBO exposes four position/radius, controls, color/source, and box-projection records plus blend controls, and deferred, legacy-forward, and WBOIT environment lighting loop over the selected probes, compute sphere+box influence, normalize local radiance across the selected set, and still reuse the current renderer-owned built-in cubemap source. CSV now reports `reflection_probe_selected_probe_count`, `reflection_probe_blended_probe_count`, `reflection_probe_max_blend_weight`, `reflection_probe_total_blend_weight`, and `reflection_probe_multi_blend_enabled`; ImGui shows selected/blended/dropped/top probe indices; FrameGraph describes `SceneReflectionProbeSelection` as a top-N selection resource. `out/benchmarks/aaa_reflection_probe_multi_blend_single_smoke.csv` reports one scene probe, one eligible probe, selected `1`, blended `1`, dropped `0`, multi blend `1`, shader sampling `1`, and 0 validation issues. `out/benchmarks/aaa_reflection_probe_multi_blend_three_smoke.csv` reports three eligible probes, selected `3`, dropped `0`, shader sampling `1`, and 0 validation issues. `out/benchmarks/aaa_reflection_probe_multi_blend_overflow_smoke.csv` uses `SE_SCENE_REFLECTION_PROBE_OVERFLOW=1` and reports six eligible probes, selected `4`, dropped `2`, shader sampling `1`, and 0 validation issues. `out/benchmarks/aaa_reflection_probe_multi_blend_wboit_smoke.csv` proves the multi-blend UBO/shader path survives deferred-HDR WBOIT with selected `3`, 5 weighted-translucency draws, one WBOIT resolve, shader sampling `1`, and 0 validation issues. `out/benchmarks/aaa_reflection_probe_multi_blend_authored_fallback_smoke.csv` forces authored cubemap source and reports selected `4`, source `2`, ready `0`, fallback reason `2`, shader sampling `0`, and 0 validation issues. This is still a shared-cubemap multi-probe scaffold, not per-probe cubemap arrays, authored cubemap import, dynamic scene capture, or final capture priority volumes.
- Latest height/distance fog smokes add the first analytic atmosphere tier across deferred, legacy forward, and WBOIT shading. The frame UBO now carries `heightFogControls` and `heightFogColor`, `SE_HEIGHT_FOG=1/0` toggles the path, ImGui exposes density/falloff/start/max/color controls, `SE_RENDER_VIEW=height-fog` visualizes fog factor through the deferred debug path, CSV reports `height_fog_*`, and the frame graph adds `HeightFogIntegrated` when enabled. `out/benchmarks/aaa_height_fog_debug_smoke.csv` reports matching 329-column rows, `height_fog_enabled=1`, density `0.035`, falloff `0.08`, start distance `3`, max opacity `0.72`, 15 active frame-graph passes, one deferred PBR debug draw, and `height_fog_debug_draws=1`. `out/benchmarks/aaa_height_fog_off_smoke.csv` keeps the debug route alive while reporting `height_fog_enabled=0`, 14 active passes, and `height_fog_debug_draws=1`, proving the feature is explicitly gated. `out/benchmarks/aaa_height_fog_wboit_smoke.csv` uses transparent benchmark material with deferred HDR and reports `height_fog_enabled=1`, 79 weighted-translucency draws, one WBOIT resolve, one transparent material, one deferred-lighting draw, and one HDR composite draw, proving the fog UBO layout and shader path survive the WBOIT transparency carrier. This is a first analytic height/distance fog tier, not full volumetric fog, aerial perspective, cloud media, volumetric shadows, or temporal reprojection.
- Latest bloom smokes promote bloom from the old HDR-composite kernel into a first production downsample/upsample pyramid. The frame UBO still carries `postProcessControls`, `SE_BLOOM=1/0` gates the feature, `SE_BLOOM_INTENSITY`, `SE_BLOOM_THRESHOLD`, and `SE_BLOOM_RADIUS` tune it for smoke runs, ImGui exposes bloom controls under Render Debug, `SE_RENDER_VIEW=bloom` visualizes the bloom-only contribution through the HDR composite path, CSV reports both `bloom_*` controls and `bloom_pyramid_*`/draw-bind counters, and the frame graph now records explicit `BloomDownsample` and `BloomUpsample` passes plus a `BloomPyramid` resource when enabled. `out/benchmarks/aaa_bloom_pyramid_debug_smoke.csv` reports `bloom_enabled=1`, `bloom_pyramid_enabled=1`, `bloom_pyramid_mip_count=4`, 4 downsample draws, 3 upsample draws, one bloom debug draw, and 0 frame-graph validation issues. `out/benchmarks/aaa_bloom_pyramid_off_smoke.csv` reports `bloom_enabled=0`, `bloom_pyramid_enabled=0`, 0 downsample/upsample draws, and 0 validation issues while keeping HDR composite sampling safe through the disabled-path clear/transition. `out/benchmarks/aaa_bloom_pyramid_deferred_hdr_smoke.csv` uses the normal `deferred-hdr` view with bloom enabled and reports the same pyramid draw counts with `bloom_debug_draws=0`, proving bloom feeds the visible composite path rather than only the debug view. `out/benchmarks/aaa_bloom_pyramid_auto_exposure_smoke.csv` proves the pyramid coexists with GPU histogram auto exposure. This is a first production bloom-pyramid carrier; dirt-mask/lens bloom, anamorphic bloom, artist quality tiers, temporal polish, and captured reference baselines remain follow-up work.
- Latest tone-map/exposure smokes add explicit HDR composite tone-map controls. The frame UBO now carries `toneMappingControls`, `SE_EXPOSURE` controls exposure, `SE_TONEMAP_MODE=aces/reinhard/linear` selects the curve, `SE_TONEMAP_WHITE_POINT` tunes Reinhard white point, ImGui exposes tone-map mode and white point under Render Debug, `SE_RENDER_VIEW=tone-map` visualizes the tone-mapped pre-grading result, CSV reports `tone_mapping_*`, and the frame graph adds `ToneMappingIntegrated` when ACES/Reinhard is enabled. `out/benchmarks/aaa_tone_map_reinhard_debug_smoke.csv` reports matching 350-column rows, `tone_mapping_enabled=1`, mode `1`, exposure `1.65`, white point `5.5`, 15 active frame-graph passes, one HDR composite draw, and `tone_mapping_debug_draws=1`. `out/benchmarks/aaa_tone_map_linear_fallback_smoke.csv` uses `SE_TONEMAP_MODE=linear`, keeps the debug route alive, reports `tone_mapping_enabled=0`, mode `2`, exposure `0.8`, 14 active passes, one HDR composite draw, and `tone_mapping_debug_draws=1`, proving the fallback is measurable and explicitly gated. `out/benchmarks/aaa_tone_map_deferred_hdr_smoke.csv` uses normal `deferred-hdr` with ACES, reports `tone_mapping_enabled=1`, mode `0`, exposure `1.35`, 15 active passes, one HDR composite draw, and `tone_mapping_debug_draws=0`, proving the controls are integrated into the visible composite path rather than only the debug view. This is an explicit curve/exposure tier, not auto exposure, histogram metering, eye adaptation, per-volume exposure, or a full post-process graph.
- Latest auto-exposure smokes promote exposure from the earlier sampled HDR-scene estimate into a GPU histogram and eye-adaptation history carrier. The frame UBO carries `autoExposureControls`, `SE_AUTO_EXPOSURE=1/0` gates the feature, `SE_AUTO_EXPOSURE_TARGET`, `SE_AUTO_EXPOSURE_MIN`, `SE_AUTO_EXPOSURE_MAX`, and `SE_AUTO_EXPOSURE_ADAPTATION` tune metering/history response, ImGui exposes controls under Render Debug, `SE_RENDER_VIEW=auto-exposure` visualizes the normalized exposure factor, CSV reports `auto_exposure_*` plus histogram/history fallback metrics, and the frame graph includes the auto-exposure integration evidence when enabled. `out/benchmarks/aaa_auto_exposure_histogram_debug_smoke.csv`, `out/benchmarks/aaa_auto_exposure_histogram_off_smoke.csv`, `out/benchmarks/aaa_auto_exposure_histogram_deferred_hdr_smoke.csv`, and `out/benchmarks/aaa_auto_exposure_histogram_history_smoke.csv` prove the enabled, disabled, normal deferred-HDR, and history/update paths with 0 GPU fallbacks in the enabled paths. This is the first GPU metering/history carrier; percentile clipping, exposure compensation curves, per-volume exposure, and stronger artist controls remain follow-up work.
- Latest color-grading smokes add the first renderer-owned neutral LUT carrier on top of the display-referred grading tier inside HDR composite. The frame UBO now carries both `colorGradingControls` and `colorGradingLutControls`, `SE_COLOR_GRADING=1/0` toggles the path, `SE_COLOR_GRADING_LUT_STRENGTH` tunes LUT blending, ImGui exposes LUT strength under Render Debug, HDR composite binds the generated 16x16x16 LUT strip when enabled, CSV reports `color_grading_lut_*`, and the frame graph exposes `ColorGradingLUT` only when the LUT is physically allocated and sampled. `out/benchmarks/aaa_color_grading_lut_debug_smoke.csv` reports matching 427-column rows, `color_grading_enabled=1`, `color_grading_lut_enabled=1`, LUT size `16`, strength `1`, 0 LUT fallbacks, 17 active frame-graph passes, 0 validation issues, and `color_grading_debug_draws=1`. `out/benchmarks/aaa_color_grading_lut_off_smoke.csv` keeps the debug route alive while reporting grading/LUT disabled, 16 active passes, and 0 validation issues. `out/benchmarks/aaa_color_grading_lut_deferred_hdr_smoke.csv` uses the normal `deferred-hdr` view with LUT strength `0.85`, reports LUT enabled with 0 fallbacks, 17 active passes, and no debug draw, proving the LUT path is integrated into the visible composite path rather than only the debug view. `out/benchmarks/aaa_color_grading_lut_bloom_auto_exposure_smoke.csv` proves the LUT carrier coexists with bloom pyramid and GPU histogram auto exposure by reporting 4 bloom downsample draws, 3 upsample draws, histogram enabled, 0 auto-exposure GPU fallbacks, LUT enabled, and 0 frame-graph validation issues. This is still a neutral generated LUT carrier, not authored LUT import, filmic look libraries, per-volume grading/blending, auto-exposure policy coupling, or final artist-authored post-process parity.
- Latest sharpening smokes add the first LDR sharpening tier inside HDR composite. The frame UBO now carries `sharpeningControls`, `SE_SHARPENING=1/0` toggles the path, `SE_SHARPENING_STRENGTH` and `SE_SHARPENING_RADIUS` tune it for smoke runs, ImGui exposes sharpening controls under Render Debug, `SE_RENDER_VIEW=sharpening` visualizes the absolute sharpening delta, CSV reports `sharpening_*`, and the frame graph adds `SharpeningIntegrated` when enabled. `out/benchmarks/aaa_sharpening_debug_smoke.csv` reports matching 364-column rows, `sharpening_enabled=1`, strength `0.8`, radius `1.25`, 16 active frame-graph passes, one HDR composite draw, and `sharpening_debug_draws=1`. `out/benchmarks/aaa_sharpening_off_smoke.csv` keeps the debug route alive while reporting `sharpening_enabled=0`, 15 active passes, one HDR composite draw, and `sharpening_debug_draws=1`, proving the feature is measurable and explicitly gated. `out/benchmarks/aaa_sharpening_deferred_hdr_smoke.csv` uses the normal `deferred-hdr` view with sharpening enabled, reports strength `0.55`, radius `1`, 16 active passes, one HDR composite draw, and `sharpening_debug_draws=0`, proving sharpening is integrated into the visible composite path rather than only the debug view. This is a first low-cost LDR unsharp-mask tier whose neighbor taps intentionally resample the display-referred composite without bloom; it is not production CAS/RCAS, not TAAU-coupled sharpening, and not final post-process graph quality.
- Latest UE SkyLight reflection-fallback smokes prove the bridge no longer misclassifies `SkyLightComponent` as a point light. `out/benchmarks/aaa_ue_bridge_skylight_reflection_fallback_smoke.csv` reports `ue_bridge_sky_light_count=1`, `ue_bridge_sky_light_applied=1`, `reflection_probe_fallback_enabled=1`, diffuse/specular fallback intensity `1`, `ue_bridge_mesh_instance_loaded_count=3`, and `ue_bridge_visual_parity_ready=1`; the runtime log reports `directional=1 point=0 spot=0 rect=0 skylight=1 skipped=0` plus `UE bridge SkyLight applied to reflection fallback`. `out/benchmarks/aaa_ue_bridge_skylight_reflection_fallback_off_smoke.csv` sets `SE_REFLECTION_PROBE_FALLBACK=0` and reports the same SkyLight count with `ue_bridge_sky_light_applied=0` and `reflection_probe_fallback_enabled=0`, proving SkyLight use is measurable and opt-out instead of hard-wired. This is still a global ambient/reflection fallback derived from UE SkyLight color/intensity, not imported UE cubemap captures or local probe blending.
- Next renderer-first slices: start filling the still-missing mainstream 3A systems rather than returning to UE scene work. Near-term candidates are real reflection-capture/probe blending on top of the new IBL carriers, authored/per-volume color grading on top of the neutral LUT carrier, volumetric fog/aerial perspective beyond the first analytic fog tier, GTAO/SSR history and denoise, TAA/velocity correctness, dynamic-resolution/upscaler scaffolding with CAS/RCAS or TAAU-coupled sharpening, classic LOD/Hi-Z visibility, bloom quality polish, and renderer-owned visual-diff/reference captures. Keep finishing shadow quality in parallel through CSM stability captures, local-shadow invalidation/bias/filter evidence, contact-shadow stability, and WBOIT/forward-reference parity. Deferred/HDR should become the default only after imported-model/material parity, lighting/IBL, shadow/probe/SSR/AO coverage, translucency, temporal/post stability, visibility scaling, and diagnostics are all strong enough.

Current renderer answer: do not make deferred/HDR the default yet. It is now visible through `Deferred HDR` and component views, and its inputs are inspectable through GBuffer/debug views, but legacy forward remains the default reference until imported-model parity, light data ownership, lighting quality, shadow/probe/SSR/AO coverage, translucency behavior, temporal/post stability, visibility scaling, and diagnostics are strong enough. The current plan is not complete until the mainstream 3A renderer feature set is implemented, benchmarked, and debuggable; UE project preview remains frozen until then.
